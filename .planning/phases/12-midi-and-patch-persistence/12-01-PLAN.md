---
phase: 12-midi-and-patch-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/domain/dx7/persistence/saved-document.ts
  - src/app/domain/dx7/persistence/parse-saved-document.ts
  - src/app/domain/dx7/persistence/parse-saved-document.spec.ts
  - src/app/core/persistence/storage.token.ts
  - src/app/core/persistence/testing/fake-storage.ts
  - src/app/state/saved-document-store.ts
  - src/app/state/saved-document-store.spec.ts
  - src/app/state/playground-patch-slot.ts
  - src/app/state/playground-patch-slot.spec.ts
  - src/app/state/lesson-progress.ts
  - src/app/state/lesson-progress.spec.ts
  - src/app/state/instrument-state.ts
  - src/app/state/instrument-state.spec.ts
  - src/app/app.ts
  - src/app/app.spec.ts
  - src/app/features/learn/learn.spec.ts
autonomous: true
requirements: [PERSIST-01]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A learner who marks a lesson complete, then constructs a fresh `LessonProgress` against the same `STORAGE` contents, still sees that lesson complete — progress survives a simulated reload (D-18, PERSIST-01)."
    - "Missing storage hydrates `defaultSavedDocument()` without throwing; present-but-unparseable storage is replaced with defaults, a recovery explanation is set, and the app stays up (D-26)."
    - "A hostile seventh operator key, a missing or non-1 schema version, an array root, a Dexed-shaped payload, and a throwing getter all make `parseSavedDocument` return `null` and throw nothing (D-26, D-27, D-28)."
    - "Unknown extra JSON keys on an otherwise valid schema-version-1 document are ignored and the document still parses (D-27)."
    - "`PlaygroundPatchSlot.write` persists a dedicated playground patch field; `LessonProgress.markComplete` does not change that field (D-21, D-22)."
    - "`InstrumentState.replacePatch` atomically replaces algorithm, six operators, and feedback after validation, and never writes storage (Phase 3 D-05)."
    - "Quota failure on `setItem` keeps the in-memory document and sets a non-destructive explanation rather than throwing into the UI (Pitfall 6)."
  artifacts:
    - src/app/domain/dx7/persistence/saved-document.ts
    - src/app/domain/dx7/persistence/parse-saved-document.ts
    - src/app/domain/dx7/persistence/parse-saved-document.spec.ts
    - src/app/core/persistence/storage.token.ts
    - src/app/core/persistence/testing/fake-storage.ts
    - src/app/state/saved-document-store.ts
    - src/app/state/saved-document-store.spec.ts
    - src/app/state/playground-patch-slot.ts
    - src/app/state/playground-patch-slot.spec.ts
    - "src/app/state/lesson-progress.ts (extended: `replaceCompleted`, persist after `markComplete`)"
    - "src/app/state/instrument-state.ts (extended: `replacePatch`)"
    - "src/app/app.ts (extended: inject `SavedDocumentStore` and call `hydrateLive()`)"
  key_links:
    - "`STORAGE.getItem(PERSISTENCE_STORAGE_KEY)` -> `parseSavedDocument` -> `SavedDocumentStore` document signal -> `LessonProgress.replaceCompleted` / `PlaygroundPatchSlot.read`"
    - "`LessonProgress.markComplete` -> `SavedDocumentStore.persistLessonProgress` -> `STORAGE.setItem` of the whole schema-version-1 document"
    - "`PlaygroundPatchSlot.write` -> `SavedDocumentStore.writePlaygroundPatch` -> the `playgroundPatch` field only; lesson completion is a different field"
    - "`parseSavedDocument` rebuilds operator keys by iterating `OPERATOR_IDS` and reading `raw[String(id)]`, then `validateOperatorParameters` / `validateFeedbackLevel` / `isAlgorithmId` / `isLessonId`"
    - "`app.spec.ts`, `lesson-progress.spec.ts`, and `learn.spec.ts` TestBeds provide `{ provide: STORAGE, useValue: new FakeStorage() }` so constructing App or calling `markComplete` never uses origin storage"
  prohibitions:
    - statement: "MUST NOT persist A/B snapshot slots, AudioNodes, or in-flight note state. A serialized SnapshotSlots blob would freeze session-only compare tools into durable storage and violate Phase 3 D-05."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT accept Dexed banks, SysEx dumps, ROM dumps, or any document whose schema version is missing or not the number 1. Importing a copyrighted bank would violate the project licence even if the bytes happened to parse."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT throw out of parseSavedDocument or SavedDocumentStore construction for malformed JSON, hostile getters, or quota errors. A crash on first load of corrupt storage is a denial of the whole instrument."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT wipe the entire origin storage collection when clearing this app's document. Other keys on the origin are not this product's to delete."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT let LessonProgress.markComplete write the Playground patch field. Completing a lesson must not destroy a saved Playground sound."
      status: flagged-unverified
      verification: test
---

<objective>
Ship schema-version-1 persistence end to end: a never-throw codec, a fakeable
`STORAGE` seam, a root `SavedDocumentStore` that hydrates lesson progress across
a simulated reload, a dedicated Playground patch slot isolated from live
`InstrumentState`, and `replacePatch` for later Playground restore / Settings
import.

This is the persistence tracer. MIDI is plan 12-02. Settings UI is plan 12-04.
Playground/ToolsPanel call sites are plan 12-03.

Purpose: close PERSIST-01's durable document, malformed-data recovery, and
Playground-slot isolation at the facade layer (D-18, D-19, D-21, D-24, D-26,
D-27, D-28).
Output: domain codec + storage adapter + store + slot + `replacePatch`, all
tested with fakes (no real origin writes required).

Checker revision: file-count and token-estimate warnings accepted; tracer kept intact (do not split).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-midi-and-patch-persistence/12-CONTEXT.md
@.planning/phases/12-midi-and-patch-persistence/12-RESEARCH.md
@.planning/phases/12-midi-and-patch-persistence/12-PATTERNS.md
@.planning/phases/12-midi-and-patch-persistence/12-VALIDATION.md
@CLAUDE.md

<interfaces>
From src/app/domain/dx7/models/patch.ts:
- export interface InstrumentPatch { readonly algorithmId: AlgorithmId; readonly operators: OperatorParameterSet; readonly feedback: number }
- export const DEFAULT_PATCH: InstrumentPatch
- export function validateFeedbackLevel(level: number): void

From src/app/domain/dx7/models/operator-parameters.ts:
- export function validateOperatorParameters(changes: Partial<OperatorParameters>): void

From src/app/domain/dx7/models/operator.ts:
- export const OPERATOR_IDS: readonly OperatorId[]
- export function isOperatorId(value: number): value is OperatorId

From src/app/domain/dx7/models/algorithm.ts:
- export function isAlgorithmId(value: number): value is AlgorithmId

From src/app/domain/dx7/lessons/lesson-definition.ts:
- export function isLessonId(value: string): value is LessonId
- export type LessonId
- export const LESSON_IDS

From src/app/core/browser/animation-frame.token.ts:
- InjectionToken with a Like interface and a no-op fallback (never null) — copy this posture for STORAGE, not AUDIO_CONTEXT_CTOR's null.

From src/app/domain/dx7/dsp/worklet-messages.ts:
- exact-six-key operator record: Object.keys length === OPERATOR_IDS.length and OPERATOR_IDS.every reading value[String(id)]. Duplicate this rule in the persistence codec. Do not import the DSP helper (it is not exported).
</interfaces>
</context>

## Artifacts this phase produces (introduced by this plan)

New files:
- `src/app/domain/dx7/persistence/saved-document.ts`
- `src/app/domain/dx7/persistence/parse-saved-document.ts`
- `src/app/domain/dx7/persistence/parse-saved-document.spec.ts`
- `src/app/core/persistence/storage.token.ts`
- `src/app/core/persistence/testing/fake-storage.ts`
- `src/app/state/saved-document-store.ts`
- `src/app/state/saved-document-store.spec.ts`
- `src/app/state/playground-patch-slot.ts`
- `src/app/state/playground-patch-slot.spec.ts`

New exported symbols:
- `PERSISTENCE_SCHEMA_VERSION` — `1` (`saved-document.ts`)
- `PERSISTENCE_STORAGE_KEY` — `'dx7-algorithm-lab.saved-document'` (`saved-document.ts`)
- `EXPORT_FILENAME` — `'dx7-algorithm-lab-backup.json'` (`saved-document.ts`)
- `MAX_IMPORT_BYTES` — `262144` (`saved-document.ts`, Assumption A1)
- `SavedDocument` (`saved-document.ts`)
- `defaultSavedDocument()` (`saved-document.ts`)
- `parseSavedDocument(data: unknown): SavedDocument | null` (`parse-saved-document.ts`)
- `StorageLike` (`storage.token.ts`)
- `STORAGE` (`storage.token.ts`)
- `FakeStorage` (`testing/fake-storage.ts`)
- `SavedDocumentStore` (`saved-document-store.ts`)
- `PlaygroundPatchSlot` (`playground-patch-slot.ts`)
- `InstrumentState.replacePatch(patch: InstrumentPatch): void`

Extended symbols:
- `LessonProgress.replaceCompleted(ids: ReadonlySet<LessonId>): void`
- `LessonProgress.markComplete` persists through `SavedDocumentStore` after a successful set write
- `App` injects `SavedDocumentStore` and calls `hydrateLive()` so hydrate always precedes user actions

<flagged_assumptions>
The spec-less probe marked PERSIST-01 unclassified. Resolved here against D-18–D-28 and `12-RESEARCH.md`, not dropped:

- **empty / malformed JSON** — missing key hydrates defaults; unparseable stored JSON resets to defaults and sets `recoveryMessage`; codec never throws.
- **hostile seventh operator** — extra operator key fails the exact-six-key rule; whole document is `null`.
- **Dexed / SysEx refuse** — missing `schemaVersion: 1` is malformed; binary-as-text and Dexed-shaped objects return `null`.
- **lesson vs Playground slot isolation** — two fields on one document; `markComplete` does not call `writePlaygroundPatch`. Component call sites land in plan 12-03.
- **Import size cap `262144`** — RESEARCH Assumption A1; named `MAX_IMPORT_BYTES`. File-picker enforcement is plan 12-04; the constant is defined here so both plans share one number.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Origin `localStorage` string -> codec | Untrusted JSON, possibly hostile getters or prototype keys |
| Imported file text -> codec | Same parser; plan 12-04 is the picker, this plan owns fail-closed decode |
| `SavedDocument` -> `LessonProgress` / slot | Hydrate must not throw; invalid ids dropped or document rejected per field rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Denial of Service | `parseSavedDocument` | high | mitigate | Whole body in try/catch; return `null`; never throw. Hostile-getter row in the spec. |
| T-12-02 | Tampering | prototype keys on imported JSON | medium | mitigate | Read only known fields; rebuild `OperatorParameterSet` from `OPERATOR_IDS`; reject arrays; never copy unknown keys onto a prototype. |
| T-12-03 | Tampering | Dexed / SysEx / ROM-shaped import | high | mitigate | `schemaVersion` must be the number `1`; D-28 refuse. |
| T-12-04 | Tampering | seventh / extra operator key | medium | mitigate | Exact six keys via `OPERATOR_IDS.length` plus `every`; extra key -> `null`. |
| T-12-05 | Denial of Service | `STORAGE.setItem` quota / private mode | medium | mitigate | Token factory no-op Like on probe failure; every store `setItem` still try/catch; live state kept; explanation signal set. |
| T-12-06 | Tampering | clearing saved data | medium | mitigate | `removeItem(PERSISTENCE_STORAGE_KEY)` only. |
| T-12-SC | Tampering | npm installs | low | accept | No packages added this phase. |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Schema-version-1 document hydrates lesson progress across a simulated reload</name>

  <reversibility rating="costly">Exported schema-version-1 files become a published shape (D-27); changing required fields later needs a migration, not a silent rewrite. Already locked in CONTEXT.md — do not re-ask.</reversibility>

  <precondition>`npm test`, `npm run build` and `npm run lint` are all green on the tree as it stands before any edit in this plan (Phase 11's shipped state). If any is red, stop and report — this plan's per-task gates cannot distinguish a pre-existing failure from one it introduces.</precondition>

  <read_first>
    - src/app/domain/dx7/models/patch.ts
    - src/app/domain/dx7/models/operator-parameters.ts
    - src/app/domain/dx7/models/operator.ts
    - src/app/domain/dx7/models/algorithm.ts
    - src/app/domain/dx7/lessons/lesson-definition.ts
    - src/app/domain/dx7/dsp/worklet-messages.ts
    - src/app/domain/dx7/dsp/worklet-messages.spec.ts
    - src/app/core/browser/animation-frame.token.ts
    - src/app/core/browser/testing/fake-animation-frame-scheduler.ts
    - src/app/state/lesson-progress.ts
    - src/app/state/lesson-progress.spec.ts
    - src/app/app.ts
    - src/app/app.spec.ts
    - src/app/features/learn/learn.spec.ts
    - .planning/phases/12-midi-and-patch-persistence/12-RESEARCH.md
    - .planning/phases/12-midi-and-patch-persistence/12-PATTERNS.md
  </read_first>

  <behavior>
    - `parseSavedDocument` returns `null` and throws nothing for: a non-object, an array root, a missing `schemaVersion`, `schemaVersion: 2`, a seventh operator key alongside the six legal ones, a Dexed-shaped object with no schema version, SysEx-as-text after JSON.parse failure is handled by the store wrapper, a throwing getter on `schemaVersion`, and a `__proto__`-only object that is not a valid document.
    - `parseSavedDocument` succeeds for a valid schema-version-1 document that also carries an unknown extra key, and the extra key is absent from the returned object.
    - `JSON.parse(JSON.stringify(defaultSavedDocument()))` round-trips: `schemaVersion` is `1`, `playgroundPatch.algorithmId` is `DEFAULT_PATCH.algorithmId`, all six operator keys are numeric `OperatorId`s after rebuild, `completedLessonIds` is `[]`, `lastMidiDeviceId` is `null`.
    - Unknown slugs inside `completedLessonIds` are dropped; remaining `isLessonId` strings are kept; the document is not rejected (Assumption A3).
    - `lastMidiDeviceId` that is `''` becomes `null`; a number or object for that field makes the whole document `null`.
    - `STORAGE` factory returns a no-op Like when the probe `setItem` throws; it never returns `null`.
    - `FakeStorage` round-trips `getItem`/`setItem`/`removeItem` in memory; `throwOnSetItem` makes `setItem` throw.
    - Missing `PERSISTENCE_STORAGE_KEY`: `SavedDocumentStore` holds `defaultSavedDocument()`, `recoveryMessage()` is `null`, no throw.
    - Present unparseable string: store writes defaults back, `recoveryMessage()` is a non-empty string, no throw (D-26).
    - `LessonProgress.markComplete('algorithm-32')` then a new TestBed with the same `FakeStorage` instance, injecting `SavedDocumentStore` then calling `hydrateLive()`, then `LessonProgress.isComplete('algorithm-32')` is `true` (D-18).
    - `setItem` throwing on persist leaves the in-memory completed set and the in-memory document as they were after the successful mark, and sets a non-empty explanation signal (Pitfall 6).
    - `clearDocument` (store method used later by Settings) uses `removeItem` of `PERSISTENCE_STORAGE_KEY` only.
  </behavior>

  <files>src/app/domain/dx7/persistence/saved-document.ts, src/app/domain/dx7/persistence/parse-saved-document.ts, src/app/domain/dx7/persistence/parse-saved-document.spec.ts, src/app/core/persistence/storage.token.ts, src/app/core/persistence/testing/fake-storage.ts, src/app/state/saved-document-store.ts, src/app/state/saved-document-store.spec.ts, src/app/state/lesson-progress.ts, src/app/state/lesson-progress.spec.ts, src/app/app.ts, src/app/app.spec.ts, src/app/features/learn/learn.spec.ts</files>

  <action>
Write the codec spec first (mirror `worklet-messages.spec.ts` `it.each` rejected rows plus a throwing-getter case) and watch it fail against the missing module. If a case is green before an implementation exists, break the implementation, confirm red, restore, and document the substitution in the SUMMARY.

**`saved-document.ts` (new, domain, zero Angular).** Export `PERSISTENCE_SCHEMA_VERSION` as `1`, `PERSISTENCE_STORAGE_KEY` as `'dx7-algorithm-lab.saved-document'`, `EXPORT_FILENAME` as `'dx7-algorithm-lab-backup.json'`, `MAX_IMPORT_BYTES` as `262144`. Export `SavedDocument` with `schemaVersion: 1`, `playgroundPatch: InstrumentPatch`, `completedLessonIds: readonly LessonId[]`, `lastMidiDeviceId: string | null`. Export `defaultSavedDocument()` returning `DEFAULT_PATCH`, `[]`, and `null`. Do not mention snapshot slots or audio types.

**`parse-saved-document.ts` (new, domain, zero Angular).** Export `parseSavedDocument(data: unknown): SavedDocument | null`. Wrap the whole body in try/catch including hostile getters; return `null` on any throw. Reject non-objects and arrays. Require `schemaVersion === 1` (D-27: missing or unknown is malformed). Ignore unknown extra keys (D-27). Rebuild `playgroundPatch.operators` by iterating `OPERATOR_IDS` and reading `raw[String(id)]` (Pitfall 4). Require `Object.keys(operators).length === OPERATOR_IDS.length`. Run `isAlgorithmId`, `validateOperatorParameters` (catch RangeError -> null), `validateFeedbackLevel` (catch -> null). `completedLessonIds` must be an array; keep only `isLessonId` strings; drop unknown slugs (A3). `lastMidiDeviceId`: `string` or `null`; empty string -> `null`; other types -> `null` document. Do not import worklet message helpers. Duplicate the exact-six-key rule locally.

**`storage.token.ts` (new).** `StorageLike` with `getItem` / `setItem` / `removeItem` only. `STORAGE` token `providedIn: 'root'`, factory mirrors `ANIMATION_FRAME_SCHEDULER`: always a Like, never `null`. Probe with try/catch `setItem`/`removeItem` of `'__dx7_storage_probe__'`. On failure return no-ops. The factory is the only file allowed to name `window.localStorage`.

**`FakeStorage` (new).** Map-backed `StorageLike`. Public `throwOnSetItem` boolean for quota tests.

**`SavedDocumentStore` (new, `providedIn: 'root'`).** Inject `STORAGE` and `LessonProgress`. Constructor reads `getItem(PERSISTENCE_STORAGE_KEY)` into a private document signal: missing -> `defaultSavedDocument()`; present but `parseSavedDocument(JSON.parse wrapped in try)` fails -> `setItem` defaults, set `recoveryMessage`. Do **not** call `replaceCompleted` in the constructor (hydrate race if `markComplete` is the first inject). Export `hydrateLive(): void` that calls `lessonProgress.replaceCompleted(new Set(doc.completedLessonIds))`. Export readonly `document`, `recoveryMessage`, `writeError`. `persistLessonProgress(ids)` updates `completedLessonIds` and `setItem` of `JSON.stringify(document)` inside try/catch; quota sets `writeError` and does not throw. `writePlaygroundPatch`, `setLastMidiDeviceId` same write path. `replaceDocument(doc)` for import (plan 12-04): parse already succeeded; setItem then `_document.set` then `hydrateLive`. `clearDocument(): void` calls `removeItem(PERSISTENCE_STORAGE_KEY)` only, then defaults + `hydrateLive`. Do not add `effect()`.

**`LessonProgress`.** Add `replaceCompleted(ids: ReadonlySet<LessonId>): void` — every id through `isLessonId` or `RangeError`; then `_completed.set(new Set(ids))`. After a successful (non-idempotent no-op) `markComplete` write, `inject(SavedDocumentStore).persistLessonProgress(this._completed())` using method-level `inject` so the store is not constructed from the field initializer. Keep the one-way ratchet for `markComplete`; `replaceCompleted` is the import/clear/hydrate path.

**`App`.** Inject `SavedDocumentStore` (same posture as `MotionPreference`) and call `hydrateLive()` from the constructor so a real boot hydrates before any click.

**`app.spec.ts`.** In every TestBed that constructs `App`, provide `{ provide: STORAGE, useValue: new FakeStorage() }` (PATTERNS.md shared TestBed pattern). Do not rely on jsdom `localStorage` for hydrate/persist.

**`lesson-progress.spec.ts`.** Provide `{ provide: STORAGE, useValue: fake }` in every setup (`fake` is a `FakeStorage`). Inject store, then `hydrateLive()`, then exercise existing ratchet tests. Add the simulated-reload case: markComplete, `TestBed.resetTestingModule()`, new module with the same FakeStorage, inject store, `hydrateLive()`, expect complete. Add `replaceCompleted` RangeError and replace-to-empty (clear) cases.

**`learn.spec.ts`.** Provide `{ provide: STORAGE, useValue: new FakeStorage() }` in every TestBed that constructs Learn so `markComplete` persist cannot leak across tests via jsdom. Do not change Learn production code.
  </action>

  <verify>
    <automated>npm test -- --include='**/parse-saved-document.spec.ts'</automated>
    <automated>npm test -- --include='**/saved-document-store.spec.ts'</automated>
    <automated>npm test -- --include='**/lesson-progress.spec.ts'</automated>
  </verify>

  <acceptance_criteria>
    - `parseSavedDocument` never throws on the hostile matrix listed in this task's behavior; each rejected row returns `null`.
    - A valid extra-key document parses; the returned object has only the four `SavedDocument` fields.
    - `JSON.parse(JSON.stringify(defaultSavedDocument()))` through `parseSavedDocument` yields numeric operator keys and `schemaVersion === 1`.
    - Simulated reload: `LessonProgress.isComplete('algorithm-32')` is true after `hydrateLive()` on a second TestBed sharing one `FakeStorage`.
    - `app.spec.ts`, `lesson-progress.spec.ts`, and `learn.spec.ts` each include `{ provide: STORAGE, useValue: new FakeStorage() }` (or `useValue: fake` bound to a `FakeStorage`) in every TestBed that constructs App, LessonProgress, or Learn.
    - Corrupt stored string: `recoveryMessage()` is non-empty and `document()` equals `defaultSavedDocument()` structurally.
    - `grep -v '^#' src/app/state/saved-document-store.ts | grep -c 'localStorage.clear'` reports `0`.
    - `grep -v '^#' src/app/domain/dx7/persistence/parse-saved-document.ts | grep -c 'from .*dsp'` reports `0`.
    - Domain files under `src/app/domain/dx7/persistence/` import no `@angular/*` (existing DOMAIN-04 lint gate).
  </acceptance_criteria>

  <done>Schema-version-1 codec, STORAGE seam, store hydrate/persist, and durable LessonProgress are committed and proven by a simulated reload plus the hostile parse matrix (D-18, D-24, D-26, D-27, D-28).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dedicated Playground slot and atomic replacePatch, isolated from lesson progress</name>

  <reversibility rating="costly">Playground slot isolation (D-21) is the contract later Playground restore and lesson startingPatch both depend on; dropping it would make lessons destroy saved Playground sounds.</reversibility>

  <read_first>
    - src/app/state/instrument-state.ts
    - src/app/state/instrument-state.spec.ts
    - src/app/state/saved-document-store.ts
    - src/app/state/lesson-progress.ts
    - src/app/domain/dx7/models/patch.ts
    - .planning/phases/12-midi-and-patch-persistence/12-PATTERNS.md
  </read_first>

  <behavior>
    - `PlaygroundPatchSlot.read()` returns `DEFAULT_PATCH` when the store holds defaults.
    - `PlaygroundPatchSlot.write(patch)` after `validateOperatorParameters` on all six ids and `validateFeedbackLevel` updates only `document().playgroundPatch`; `completedLessonIds` is unchanged.
    - `LessonProgress.markComplete` after a prior `write` leaves `read()` equal to the written patch (D-21, D-22).
    - `write` of an invalid feedback throws `RangeError` and does not change `read()`.
    - `InstrumentState.replacePatch(DEFAULT_PATCH)` after `setAlgorithm(32)` restores `algorithmId` 1, default operators, feedback 0 in one `_patch.set`.
    - `replacePatch` with algorithmId `99` throws `RangeError` and leaves the previous patch reference.
    - `replacePatch` does not change `snapshots()` (same isolation as `reset`).
    - `replacePatch` does not import the persistence adapter token or call `PlaygroundPatchSlot`.
  </behavior>

  <files>src/app/state/playground-patch-slot.ts, src/app/state/playground-patch-slot.spec.ts, src/app/state/instrument-state.ts, src/app/state/instrument-state.spec.ts</files>

  <action>
**`PlaygroundPatchSlot` (new, `providedIn: 'root'`).** Inject `SavedDocumentStore`. `read(): InstrumentPatch` returns `store.document().playgroundPatch`. `write(patch: InstrumentPatch): void` validates every `OPERATOR_IDS` entry with `validateOperatorParameters`, then `validateFeedbackLevel`, then `store.writePlaygroundPatch(patch)`. No `effect()`. No subscription to `InstrumentState.patch()`. Call-site origin is the rule (RESEARCH Pattern 3): this plan does not wire ToolsPanel; that is 12-03.

**`InstrumentState.replacePatch(patch: InstrumentPatch): void`.** Call existing `resolveAlgorithm(patch.algorithmId)` first (throws `RangeError` on unknown id). Validate all six operators and feedback, then one `_patch.set` of a new object `{ algorithmId, operators, feedback }` — include `algorithmId`, unlike `randomize` which spreads previous. Do not touch `_snapshots`. Do not read the persistence adapter from this facade. Extend `instrument-state.spec.ts` next to the existing `reset` cases.

**`playground-patch-slot.spec.ts`.** Provide `STORAGE: FakeStorage`. Prove write/read, isolation from `markComplete`, and validation throw. Do not instantiate Playground or LessonDetail.
  </action>

  <verify>
    <automated>npm test -- --include='**/playground-patch-slot.spec.ts'</automated>
    <automated>npm test -- --include='**/instrument-state.spec.ts'</automated>
  </verify>

  <acceptance_criteria>
    - Slot write then `markComplete` then `read()` still equals the written patch.
    - `replacePatch` restores a full `InstrumentPatch` including `algorithmId` in one write, asserted by a spec that `setAlgorithm(32)` then `replacePatch(DEFAULT_PATCH)` yields `algorithmId === 1`.
    - After `captureSnapshot('a')` then `replacePatch(DEFAULT_PATCH)`, `hasSnapshot('a')` remains true.
    - `grep -v '^#' src/app/state/instrument-state.ts | grep -c 'core/persistence'` reports `0`.
    - `grep -v '^#' src/app/state/instrument-state.ts | grep -c SnapshotSlots` still reports a type/export presence (snapshots remain in-memory); `replacePatch` body does not assign `_snapshots`.
  </acceptance_criteria>

  <done>PlaygroundPatchSlot is a storage-backed field isolated from lesson progress, and replacePatch is the atomic live-state apply used by later Playground restore and Settings import (D-19, D-21, D-22).</done>
</task>

</tasks>

<verification>
- `npm test -- --include='**/parse-saved-document.spec.ts'`, `saved-document-store.spec.ts`, `lesson-progress.spec.ts`, `playground-patch-slot.spec.ts`, and `instrument-state.spec.ts` pass.
- `npm run build` and `npm run lint` pass before the plan is closed.
- No Angular imports under `src/app/domain/dx7/persistence/`.
</verification>

<success_criteria>
- Lesson completion survives a simulated reload through the versioned document.
- Malformed stored JSON recovers to defaults without throwing.
- The Playground patch field is a dedicated slot, not live `InstrumentState`.
</success_criteria>

<output>
Create `.planning/phases/12-midi-and-patch-persistence/12-01-SUMMARY.md` when done
</output>
