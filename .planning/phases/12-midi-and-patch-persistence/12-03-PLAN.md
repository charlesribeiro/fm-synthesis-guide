---
phase: 12-midi-and-patch-persistence
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - src/app/features/playground/playground.ts
  - src/app/features/playground/playground.spec.ts
  - src/app/features/playground/tools-panel/tools-panel.ts
  - src/app/features/playground/tools-panel/tools-panel.spec.ts
  - src/app/features/learn/lesson-detail/lesson-detail.spec.ts
autonomous: true
requirements: [PERSIST-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Entering `/playground` applies `PlaygroundPatchSlot.read()` through `InstrumentState.replacePatch`, so a lesson `startingPatch` left in live state does not remain on Playground (D-21)."
    - "ToolsPanel Randomize and Reset each call `PlaygroundPatchSlot.write(state.patch())` after the live command (D-22)."
    - "ToolsPanel Capture/Recall A/B do not call `write` (D-19)."
    - "Opening a lesson still writes `startingPatch` into live `InstrumentState` and does not call `PlaygroundPatchSlot.write` (D-21, Pitfall 5)."
    - "`LessonDetail` production code is unchanged; isolation is proven by a spy on the slot."
  artifacts:
    - "src/app/features/playground/playground.ts (extended: constructor restore from slot)"
    - "src/app/features/playground/playground.spec.ts (extended: restore-on-enter)"
    - "src/app/features/playground/tools-panel/tools-panel.ts (extended: write slot after randomize/reset)"
    - "src/app/features/playground/tools-panel/tools-panel.spec.ts (extended)"
    - "src/app/features/learn/lesson-detail/lesson-detail.spec.ts (extended: slot write spy)"
  key_links:
    - "`/playground` constructor -> `PlaygroundPatchSlot.read` -> `InstrumentState.replacePatch`"
    - "`ToolsPanel.resetPatch` / `randomizePatch` -> live command -> `PlaygroundPatchSlot.write`"
    - "`LessonDetail.applyStartingPatch` -> `setAlgorithm` / `updateOperator` / `setFeedback` only"
  prohibitions:
    - statement: "MUST NOT persist live InstrumentState from a global subscription or effect. A lesson startingPatch sync would then overwrite the saved Playground sound."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT serialize A/B snapshot slots when writing the Playground slot. Capture/Recall remain session-only (D-19, Phase 3 D-05)."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT add a reactive route-sync hook on Playground to restore the slot. Constructor restore matches loadComponent's new-instance-per-navigation (Assumption A5)."
      status: flagged-unverified
      verification: test
---

<objective>
Connect the Playground slot shipped in plan 12-01 to the actual Playground
and ToolsPanel call sites, and prove lessons cannot write that slot.

Purpose: D-21/D-22 isolation a learner can feel — open a lesson, come back to
Playground, hear the saved Playground patch, not the lesson preset.
Output: Playground restore-on-enter, ToolsPanel write-on-randomize/reset,
lesson-detail spy proving no slot write.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-midi-and-patch-persistence/12-01-PLAN.md
@.planning/phases/12-midi-and-patch-persistence/12-CONTEXT.md
@.planning/phases/12-midi-and-patch-persistence/12-PATTERNS.md
@CLAUDE.md

<interfaces>
From plan 12-01:
- PlaygroundPatchSlot.read(): InstrumentPatch
- PlaygroundPatchSlot.write(patch: InstrumentPatch): void
- InstrumentState.replacePatch(patch: InstrumentPatch): void
- STORAGE + FakeStorage must be provided in any TestBed that constructs Playground, ToolsPanel, or LessonDetail (they now reach SavedDocumentStore). `lesson-detail.spec.ts` uses `{ provide: STORAGE, useValue: new FakeStorage() }`.
</interfaces>
</context>

## Artifacts this phase produces (introduced by this plan)

New files: none.

New exported symbols: none.

Extended symbols:
- `Playground` constructor restores the Playground slot into live `InstrumentState`
- `ToolsPanel.resetPatch` / `randomizePatch` persist the resulting patch to the slot

<flagged_assumptions>
- **lesson vs Playground slot isolation (component layer)** — probe item for PERSIST-01. Plan 12-01 proved field isolation on the store; this plan proves call-site isolation: LessonDetail never calls `write`; Playground constructor always `replacePatch(read())`.
- **Assumption A5** — constructor restore is enough because `loadComponent` creates a new Playground instance per navigation. If reuse is later observed, restore moves to a route hook, still not an `InstrumentState` effect.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lesson `startingPatch` -> live `InstrumentState` | Trusted in-app data; must not cross into the durable Playground field |
| ToolsPanel commands -> slot | Only Randomize/Reset (and future Playground editors) are Playground-originated writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-12 | Tampering | LessonDetail -> Playground slot | medium | mitigate | No `write` from lesson code; spy in `lesson-detail.spec.ts`. |
| T-12-13 | Tampering | global `patch()` subscription | medium | mitigate | Call-site writes only; Playground constructor reads, never writes. |
| T-12-SC | Tampering | npm installs | low | accept | No packages. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Playground restore-on-enter and ToolsPanel slot writes</name>

  <reversibility rating="costly">Playground restore-on-enter and lesson startingPatch sync both depend on this isolation (D-21).</reversibility>

  <read_first>
    - src/app/features/playground/playground.ts
    - src/app/features/playground/playground.spec.ts
    - src/app/features/playground/tools-panel/tools-panel.ts
    - src/app/features/playground/tools-panel/tools-panel.spec.ts
    - src/app/state/playground-patch-slot.ts
    - src/app/state/instrument-state.ts
    - .planning/phases/12-midi-and-patch-persistence/12-PATTERNS.md
  </read_first>

  <behavior>
    - A TestBed that writes a patch with `algorithmId` 32 into the slot, then `setAlgorithm(1)` on live state, then constructs `Playground`, ends with `instrumentState.algorithmId() === 32`.
    - Clicking Reset on ToolsPanel leaves `PlaygroundPatchSlot.read()` equal to `DEFAULT_PATCH`.
    - Clicking Randomize leaves `read().operators` equal to the live `patch().operators` (same object reference after write).
    - Clicking Capture A does not change `read()` compared to before the click.
    - `playground.ts` contains no `effect(` call.
  </behavior>

  <files>src/app/features/playground/playground.ts, src/app/features/playground/playground.spec.ts, src/app/features/playground/tools-panel/tools-panel.ts, src/app/features/playground/tools-panel/tools-panel.spec.ts</files>

  <action>
**Playground.** Inject `InstrumentState` and `PlaygroundPatchSlot`. In the constructor, `this.instrumentState.replacePatch(this.playgroundPatchSlot.read())`. Do not write the slot. Do not derive restore from a route signal. Do not subscribe to `router.events`. Keep the component a thin host otherwise.

**ToolsPanel.** Inject `PlaygroundPatchSlot`. After `state.reset()` in `resetPatch`, call `write(this.state.patch())`. After `state.randomize()` in `randomizePatch`, call `write(this.state.patch())`. Capture/Recall paths stay as they are.

**Specs.** Provide `{ provide: STORAGE, useValue: new FakeStorage() }` in Playground and ToolsPanel TestBed setups (both now reach `SavedDocumentStore` through the slot). Provide `{ provide: REQUEST_MIDI_ACCESS, useValue: null }` so Playground's embedded PlaySurface / `MidiSession` (plan 12-02, same prior wave) does not touch `navigator`. Keep existing visualizer and audio worklet fakes.

Add the restore-on-enter spec and the ToolsPanel write/non-write specs. Do not assert colour.
  </action>

  <verify>
    <automated>npm test -- --include='**/playground.spec.ts'</automated>
    <automated>npm test -- --include='**/tools-panel.spec.ts'</automated>
  </verify>

  <acceptance_criteria>
    - Restore-on-enter spec: live state after constructing Playground matches the slot, not a previously selected live algorithm (D-21).
    - Reset and Randomize each result in `PlaygroundPatchSlot.read()` matching `InstrumentState.patch()` after the click (D-22).
    - Capture A then `read()` equals the pre-capture slot value (D-19).
    - `grep -v '^#' src/app/features/playground/playground.ts | grep -c 'effect('` reports `0`.
    - Existing Playground and ToolsPanel specs still pass.
  </acceptance_criteria>

  <done>Entering Playground restores the dedicated slot; only Randomize and Reset persist it (D-19, D-21, D-22).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lesson startingPatch does not write the Playground slot</name>

  <read_first>
    - src/app/features/learn/lesson-detail/lesson-detail.ts
    - src/app/features/learn/lesson-detail/lesson-detail.spec.ts
    - src/app/state/playground-patch-slot.ts
    - src/app/core/persistence/testing/fake-storage.ts
  </read_first>

  <behavior>
    - Navigating to `/learn/algorithm-32` still applies that lesson's `startingPatch` through `setAlgorithm` / `updateOperator` / `setFeedback` (existing spec).
    - `PlaygroundPatchSlot.write` is not called during that navigation (spy call count 0).
    - `PlaygroundPatchSlot.read()` after the navigation still equals `DEFAULT_PATCH` when storage started empty.
  </behavior>

  <files>src/app/features/learn/lesson-detail/lesson-detail.spec.ts</files>

  <action>
Do **not** modify `lesson-detail.ts`. Provide `STORAGE` with a fresh `FakeStorage` in the lesson-detail TestBed so constructing the slot/store is isolated. Inject `PlaygroundPatchSlot`, `vi.spyOn(slot, 'write')`, navigate to `/learn/algorithm-32` as the existing startingPatch specs do, expect `write` not to have been called, and expect `read()` to remain `DEFAULT_PATCH`. Keep the existing startingPatch field assertions.

If MidiSession is pulled in through PlaySurface on the lesson page (plan 12-02), provide `{ provide: REQUEST_MIDI_ACCESS, useValue: null }` in this TestBed too.
  </action>

  <verify>
    <automated>npm test -- --include='**/lesson-detail.spec.ts'</automated>
  </verify>

  <acceptance_criteria>
    - Existing startingPatch application assertions still pass.
    - New spec: `PlaygroundPatchSlot.write` call count is 0 after opening Algorithm 32's lesson.
    - `lesson-detail.spec.ts` TestBed includes `{ provide: STORAGE, useValue: new FakeStorage() }` in every setup that constructs LessonDetail (SavedDocumentStore / PlaygroundPatchSlot).
    - `lesson-detail.ts` is git-unchanged in this plan (`git diff --name-only` does not list it).
  </acceptance_criteria>

  <done>Lessons still apply startingPatch to live state and never persist the Playground slot (D-21, Pitfall 5).</done>
</task>

</tasks>

<verification>
- Playground, ToolsPanel, and lesson-detail focused specs pass.
- `npm run build` and `npm run lint` pass before close-out.
</verification>

<success_criteria>
- A learner can randomize on Playground, visit a lesson, return to Playground, and hear the randomized (saved) patch.
- Completing or even opening a lesson cannot overwrite that saved patch.
</success_criteria>

<output>
Create `.planning/phases/12-midi-and-patch-persistence/12-03-SUMMARY.md` when done
</output>
