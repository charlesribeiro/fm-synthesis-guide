---
phase: 12-midi-and-patch-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/core/browser/midi-access.token.ts
  - src/app/core/midi/testing/fake-midi-access.ts
  - src/app/domain/dx7/midi/parse-midi-note-message.ts
  - src/app/domain/dx7/midi/parse-midi-note-message.spec.ts
  - src/app/core/midi/midi-session.ts
  - src/app/core/midi/midi-session.spec.ts
  - src/app/features/play-surface/play-surface.ts
  - src/app/features/play-surface/play-surface.html
  - src/app/features/play-surface/play-surface.spec.ts
autonomous: true
requirements: [MIDI-01]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Before any Enable MIDI click, `MidiSession.status()` is `'off'` (or `'unsupported'` when the token is `null`); it is never `'no-devices'` on construction (D-08, Pitfall 9)."
    - "Enable MIDI on PlaySurface requests access only from that click, starts audio on the same gesture if audio is not ready, and leaves `document.activeElement` on the Enable MIDI button (D-06, D-08, D-10)."
    - "A MIDI note-on with velocity 1–127 goes through `pressKey(note, velocity)` so `notePlayed` fires; velocity 0 and status `0x80` go through `releaseKey` and never call `engine.noteOn` with velocity `0` (D-14, D-16, Pitfall 2)."
    - "CC, pitch bend, program change, clock, and SysEx parse to `null` and change no hold count (D-11)."
    - "Notes 0–127 sound; on-screen keys still only light for 60–71 (D-12, D-15)."
    - "MIDI plus pointer sharing C4 keep the note held until both owners release (D-17); a new MIDI note retunes the voice (D-13, existing engine last-note-wins)."
    - "Disable MIDI and selected-device disconnect both call `allNotesOff` (D-09). Disconnect of a non-selected port does not change selection (D-04)."
    - "The app stays fully usable when the MIDI token is `null`: PlaySurface still enables audio and plays from on-screen keys (MIDI-01)."
  artifacts:
    - src/app/core/browser/midi-access.token.ts
    - src/app/core/midi/testing/fake-midi-access.ts
    - src/app/domain/dx7/midi/parse-midi-note-message.ts
    - src/app/domain/dx7/midi/parse-midi-note-message.spec.ts
    - src/app/core/midi/midi-session.ts
    - src/app/core/midi/midi-session.spec.ts
    - "src/app/features/play-surface/play-surface.ts (extended: initializeAudio split, Enable MIDI, MIDI ownership)"
    - "src/app/features/play-surface/play-surface.html (extended: MIDI toggle + short status)"
    - "src/app/features/play-surface/play-surface.spec.ts (extended)"
  key_links:
    - "`REQUEST_MIDI_ACCESS` factory feature-detects; `MidiSession.enable` is the only caller of the returned function"
    - "`onmidimessage` on the selected port only -> `parseMidiNoteMessage` -> PlaySurface `pressKey`/`releaseKey` -> `SYNTH_ENGINE`"
    - "`initializeAudio()` resumes the engine without focusing a key; `enableAudio()` still focuses the first `.key` after success"
  prohibitions:
    - statement: "MUST NOT pass MIDI velocity 0 into the engine note-on path. WorkletSynthEngine.validateVelocity rejects 0 with RangeError; a controller that uses running-status note-offs would crash the lesson."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT request MIDI access at module evaluation, from a token factory invocation, or from APP_INITIALIZER. Permission is the Enable MIDI click."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT attach the selected-port message handler to every input. Omni listening is channel omni on one chosen device, not every device at once."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT move keyboard focus after Enable MIDI succeeds, including when that click also starts audio. Landing on C4 after a Settings or PlaySurface MIDI gesture traps keyboard users."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT start audio from a MIDI note-on. The gesture is Enable MIDI / Enable audio, matching AUDIO-01."
      status: flagged-unverified
      verification: test
---

<objective>
Wire one MIDI note end to end: feature-detect, Enable MIDI from PlaySurface,
parse note-on/off, own the note through the shared `pressKey`/`releaseKey`
path, then expand to the five named states, disconnect, and disable.

This is the MIDI tracer. Persistence is plan 12-01. Settings UI and last-device
id persistence are plan 12-04. `MidiSession.enable` this plan auto-selects the
first `inputs.values()` port (D-03). Plan 12-04 prefers the saved id (D-20) by
editing `enable` against the store this plan does not inject.

Purpose: close MIDI-01's progressive input, velocity, connect/disconnect, and
usable-without-MIDI clauses (D-01, D-03–D-17).
Output: MIDI token + fake + parser + session + PlaySurface MIDI ownership.

Checker revision: token-estimate warning accepted; tracer kept intact (do not split). STORAGE for PlaySurface TestBeds lands in 12-04 when MidiSession injects SavedDocumentStore.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-midi-and-patch-persistence/12-CONTEXT.md
@.planning/phases/12-midi-and-patch-persistence/12-RESEARCH.md
@.planning/phases/12-midi-and-patch-persistence/12-PATTERNS.md
@.planning/phases/12-midi-and-patch-persistence/12-VALIDATION.md
@CLAUDE.md

<interfaces>
From src/app/core/audio/audio-context.token.ts:
- Factory may read typeof a global; must not construct/call it. Tests provide the token, never navigator.

From src/app/core/audio/synth-engine.ts:
- noteOn(note: number, velocity: number): void
- noteOff(note: number): void
- allNotesOff(): void
- MIN_VELOCITY is 1 (value-conversion.ts) — engine throws RangeError on 0.

From src/app/features/play-surface/play-surface.ts:
- pressKey currently (note: number): boolean and always uses PLAYABLE_VELOCITY = 100
- enableAudio focuses the first `.key` after success — split initializeAudio() for D-10
- noteHoldCount / notePlayed / heldNotes already implement multi-owner hold

From src/app/features/play-surface/keyboard-note-map.ts:
- LOWEST_PLAYABLE_NOTE = 60, HIGHEST_PLAYABLE_NOTE = 71 — highlight range only
</interfaces>
</context>

## Artifacts this phase produces (introduced by this plan)

New files:
- `src/app/core/browser/midi-access.token.ts`
- `src/app/core/midi/testing/fake-midi-access.ts`
- `src/app/domain/dx7/midi/parse-midi-note-message.ts`
- `src/app/domain/dx7/midi/parse-midi-note-message.spec.ts`
- `src/app/core/midi/midi-session.ts`
- `src/app/core/midi/midi-session.spec.ts`

New exported symbols:
- `MidiPortLike`, `MidiInputLike`, `MidiAccessLike`, `RequestMidiAccessLike`, `MidiMessageEventLike`
- `REQUEST_MIDI_ACCESS`
- `FakeMidiInput`, `FakeMidiAccess`, `createFakeRequestMidiAccess`
- `ParsedMidiNote`, `parseMidiNoteMessage(data: Uint8Array | null): ParsedMidiNote | null`
- `MidiSessionStatus` — `'off' | 'unsupported' | 'permission-denied' | 'no-devices' | 'disconnected' | 'ready'`
- `MidiPortView` — `{ readonly id: string; readonly name: string; readonly connected: boolean }`
- `MidiSession` — `status`, `ports`, `selectedPortId`, `selectedPortName`, `enable()`, `disable()`, `selectPort(id)`, `subscribeNotes(listener): () => void`

Extended symbols:
- `PlaySurface.initializeAudio(): Promise<void>` — gesture + `engine.initialize()`, no focus
- `PlaySurface.enableAudio()` — calls `initializeAudio()` then focuses first `.key` (existing behavior)
- `PlaySurface.pressKey(note: number, velocity: number = PLAYABLE_VELOCITY)`

<flagged_assumptions>
The spec-less probe marked MIDI-01 unclassified. Resolved here against D-01–D-17:

- **unsupported MIDI** — `REQUEST_MIDI_ACCESS === null` maps to `'unsupported'` without calling the API.
- **velocity-0 note-on** — parser returns `{ kind: 'off', note }`; PlaySurface uses the release path.
- **disconnect** — selected id missing -> `'disconnected'`; same id returning -> `'ready'`; other ports ignored (D-04). Task 2.
- **empty inputs after enable** — `'no-devices'`, not `'off'`.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MIDI bytes -> parser | Untrusted `Uint8Array` from a device |
| Permission prompt -> session | `NotAllowedError` must become a named UI state, not an unhandled rejection |
| Enable MIDI click -> AudioContext | Same gesture may start audio (D-06); must not start audio from note-on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | Information Disclosure | SysEx / `requestMIDIAccess({ sysex: true })` | medium | mitigate | Always pass `{ sysex: false }`. Parser returns null for status `>= 0xF0`. |
| T-12-08 | Denial of Service | velocity 0 note-on -> `engine.noteOn` | high | mitigate | Parser rewrites to `kind: 'off'`. Spec asserts `noteOn` is never called with 0. |
| T-12-09 | Denial of Service | stuck note on disable/disconnect | medium | mitigate | `allNotesOff` on disable and selected disconnect; PlaySurface clears `midiHeldNotes`. |
| T-12-10 | Denial of Service | silent MIDI trap (audio off) | medium | mitigate | Enable MIDI calls `initializeAudio()` on the same click (D-06). |
| T-12-11 | Tampering | CC / SysEx as note | low | mitigate | Parser ignores non-note channel voice messages (D-11). |
| T-12-SC | Tampering | npm installs | low | accept | No packages; hand-rolled `MidiAccessLike` because TS 6 lib.dom has no MIDI types. |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enable MIDI and one note-on through pressKey, without stealing focus</name>

  <reversibility rating="costly">PlaySurface, lessons, and Playground share one MidiSession (D-01); splitting later means re-deriving enable/status wiring.</reversibility>

  <read_first>
    - src/app/core/audio/audio-context.token.ts
    - src/app/core/audio/testing/fake-audio-context.ts
    - src/app/core/audio/synth-engine.ts
    - src/app/core/audio/synth-engine.token.ts
    - src/app/domain/dx7/audio/value-conversion.ts
    - src/app/core/audio/worklet-synth-engine.ts
    - src/app/features/play-surface/play-surface.ts
    - src/app/features/play-surface/play-surface.html
    - src/app/features/play-surface/play-surface.spec.ts
    - src/app/features/play-surface/keyboard-note-map.ts
    - src/app/core/browser/motion-preference.ts
    - .planning/phases/12-midi-and-patch-persistence/12-RESEARCH.md
    - .planning/phases/12-midi-and-patch-persistence/12-PATTERNS.md
  </read_first>

  <behavior>
    - `parseMidiNoteMessage` on `[0x90, 60, 100]` returns `{ kind: 'on', note: 60, velocity: 100 }`.
    - `[0x90, 60, 0]` returns `{ kind: 'off', note: 60 }` with no velocity field.
    - `[0x80, 60, 64]` returns `{ kind: 'off', note: 60 }` (off-velocity ignored).
    - `[0x9F, 72, 1]` still returns on (omni, D-07). Note 72 is outside C4–B4 and is still a valid parse.
    - `[0xB0, 1, 1]`, a 2-byte buffer, note `128`, and `null` each return `null` and throw nothing.
    - Constructing `MidiSession` with a fake request that records calls has call count `0`.
    - Token `null`: `enable()` sets `'unsupported'` and does not throw.
    - Fake request resolving with one input: `enable()` sets `'ready'`, `selectedPortId` is that input's id (first `values()`).
    - PlaySurface Enable MIDI: after click, `document.activeElement` is the Enable MIDI button, not the first `.key`; existing Enable-audio-focus spec still passes when Enable audio is used.
    - Enable MIDI when audio is `suspended` constructs exactly one FakeAudioContext (D-06).
    - After Enable MIDI and a fake input `emit(Uint8Array.of(0x90, 60, 80))`, `notePlayed` emits `60` and `engine.noteOn` was called with `(60, 80)`.
    - `emit(Uint8Array.of(0x90, 60, 0))` after that on does not call `noteOn` with `0`; it follows the release path.
    - Pointer/keyboard presses still omit the velocity argument and use `PLAYABLE_VELOCITY` 100.
  </behavior>

  <files>src/app/core/browser/midi-access.token.ts, src/app/core/midi/testing/fake-midi-access.ts, src/app/domain/dx7/midi/parse-midi-note-message.ts, src/app/domain/dx7/midi/parse-midi-note-message.spec.ts, src/app/core/midi/midi-session.ts, src/app/core/midi/midi-session.spec.ts, src/app/features/play-surface/play-surface.ts, src/app/features/play-surface/play-surface.html, src/app/features/play-surface/play-surface.spec.ts</files>

  <action>
Write parser and session specs first. Domain parser: zero Angular, import `MIN_MIDI_NOTE` / `MAX_MIDI_NOTE` / `MIN_VELOCITY` / `MAX_VELOCITY` from `value-conversion.ts` — do not relitigate bounds.

**`parseMidiNoteMessage`.** High nibble `0x90`: velocity `0` -> `{ kind: 'off', note }`; velocity in `MIN_VELOCITY`..`MAX_VELOCITY` -> `{ kind: 'on', note, velocity }`; other velocities -> `null`. High nibble `0x80` -> off, ignore data[2]. Everything else including status `>= 0xF0` -> `null`. Channel nibble ignored (D-07). Buffers shorter than three bytes or `null` return `null`. Wrap in try/catch so a hostile getter on a fake array-like still returns `null`.

**`midi-access.token.ts`.** Hand-roll `MidiAccessLike` / `MidiInputLike` (`id`, `name`, `state`, `onmidimessage`) / `RequestMidiAccessLike`. `REQUEST_MIDI_ACCESS` factory: if `typeof navigator === 'undefined'` or `typeof navigator.requestMIDIAccess !== 'function'`, return `null`; else return `(options) => navigator.requestMIDIAccess(options)`. Factory must not invoke the function. Tests never assign `navigator.requestMIDIAccess`.

**`fake-midi-access.ts`.** `FakeMidiInput` with `emit(data)`. `FakeMidiAccess` with Map-backed `inputs` exposing `values()` in insertion order (D-03). `onstatechange` settable. Helpers to add/remove ports and set `state`. `createFakeRequestMidiAccess({ rejectWith?: DOMException, access?: FakeMidiAccess })`.

**`MidiSession` (`providedIn: 'root'`).** Inject `REQUEST_MIDI_ACCESS` and `SYNTH_ENGINE`. Private writable `_status` starts `'unsupported'` if token is `null`, else `'off'`. `enable()`: null token -> `'unsupported'`; if access already held, reattach handlers without a second request (RESEARCH Open Question 2); else `await request({ sysex: false })`. Map `DOMException.name === 'NotAllowedError'` to `'permission-denied'`, anything else to `'unsupported'`. Catch so the click handler sees no rejection. On success attach `access.onstatechange`, select first `inputs.values()` id (D-03), attach `onmidimessage` on **that port only**, notify `subscribeNotes` listeners with `parseMidiNoteMessage` results (skip `null`). `disable()`: drop handlers (`onmidimessage` / `onstatechange` = `null`), `engine.allNotesOff()`, status `'off'`, keep the access object for re-enable. `subscribeNotes` returns an unsubscribe. `DestroyRef` clears handlers. No `effect()`. Public `selectPort(id)` exists for Settings (12-04) and is tested in Task 2.

**PlaySurface.** Inject `MidiSession`. Split `initializeAudio()` (set `enabling`, `engine.initialize()`, no querySelector focus) from `enableAudio()` (initializeAudio then existing first-`.key` focus). Enable MIDI click: `await initializeAudio()` then `await midiSession.enable()` — never call `enableAudio()` from this path (Pitfall 1). Button label Enable MIDI when status is not `'ready'`, Disable MIDI when `'ready'` (D-09). Short status text: `'off'` -> `MIDI is off.`; `'ready'` includes `selectedPortName()` (D-05). Keep audio status paragraph as it is; add a second `role="status"` for MIDI so the two cannot disagree with a single concatenated string. `pressKey(note, velocity = PLAYABLE_VELOCITY)`. MIDI handler: maintain `midiHeldNotes`. On `on`: if not in set, add and `pressKey(note, velocity)`; if already in set, `engine.noteOn(note, velocity)` + `markHeld` + `notePlayed.emit` without incrementing `noteHoldCount`. On `off`: if in set, delete and `releaseKey`. Register `subscribeNotes` in the constructor; unsubscribe on `DestroyRef`; also clear `midiHeldNotes`. Find the MIDI button by its accessible name in specs (`Enable MIDI` / `Disable MIDI`), not `button.button--primary` (that remains Enable audio).

Provide `REQUEST_MIDI_ACCESS` in `play-surface.spec.ts` setup. Keep the Enable-audio-focus spec. Add Enable MIDI focus spec and notePlayed/velocity specs.
  </action>

  <verify>
    <automated>npm test -- --include='**/parse-midi-note-message.spec.ts'</automated>
    <automated>npm test -- --include='**/midi-session.spec.ts'</automated>
    <automated>npm test -- --include='**/play-surface.spec.ts'</automated>
  </verify>

  <acceptance_criteria>
    - Parser matrix in this task's behavior is asserted in `parse-midi-note-message.spec.ts`.
    - `MidiSession` construction does not invoke the fake request function (call count 0) (D-08).
    - After Enable MIDI, `document.activeElement` is the Enable MIDI button (D-10).
    - After Enable audio, `document.activeElement` is still the first `.key` (existing spec).
    - Fake note-on velocity 80 calls `noteOn(60, 80)` and emits `notePlayed` 60 (D-16).
    - Fake note-on velocity 0 never calls `noteOn` with `0` (D-14).
    - `enable()` passes `{ sysex: false }` as the request options object.
  </acceptance_criteria>

  <done>One MIDI note sounds through PlaySurface pressKey, Enable MIDI does not steal focus, velocity 0 is a release, and requestMIDIAccess is not called on construction (D-01, D-06, D-08, D-10, D-14, D-16).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Named MIDI states, disconnect without silent switch, disable allNotesOff, MIDI+pointer hold</name>

  <read_first>
    - src/app/core/midi/midi-session.ts
    - src/app/core/midi/midi-session.spec.ts
    - src/app/features/play-surface/play-surface.ts
    - src/app/features/play-surface/play-surface.spec.ts
    - src/app/core/midi/testing/fake-midi-access.ts
  </read_first>

  <behavior>
    - `NotAllowedError` from the request -> `'permission-denied'`.
    - Resolved access with zero inputs -> `'no-devices'`.
    - Two inputs A then B: auto-select is A's id (iterator order, D-03). `selectPort(B.id)` then disconnect A: status stays `'ready'`, selected remains B.
    - Disconnect the selected port: `'disconnected'`, `allNotesOff` called, selected id unchanged, picker-facing `ports()` still includes that id with `connected: false` (D-04).
    - Re-add the same id as connected: `'ready'`, `onmidimessage` reattached to that port.
    - `disable()` calls `allNotesOff`, status `'off'`, a later `enable()` does not increment the request-function call count (access kept).
    - CC emit after ready: `noteOn` call count unchanged.
    - MIDI holds 60, pointer also holds 60, MIDI off: key stays `aria-pressed` true until pointer up (D-17).
    - MIDI note 48 (below C4) sounds (`noteOn` called) and no `[data-note="48"]` key exists to highlight (D-12, D-15).
  </behavior>

  <files>src/app/core/midi/midi-session.ts, src/app/core/midi/midi-session.spec.ts, src/app/features/play-surface/play-surface.ts, src/app/features/play-surface/play-surface.spec.ts, src/app/core/midi/testing/fake-midi-access.ts</files>

  <action>
Extend `midi-session.spec.ts` with the D-04 matrix using fake port add/remove and `onstatechange` dispatch. Provide `{ provide: SYNTH_ENGINE, useValue: fakeEngine }` to count `allNotesOff`. Refreshing ports must not attach `onmidimessage` on non-selected inputs — assert the unselected `FakeMidiInput.onmidimessage` stays `null`.

On selected disconnect: `allNotesOff`, status `'disconnected'`, keep `selectedPortId`. Do not auto-select another connected port (D-04).

PlaySurface: when session status leaves `'ready'`, the session has already rung `allNotesOff`. Clear MIDI ownership through `releaseMidiHeldNotes`; when releasing a MIDI-held note leaves a shared hold from a pointer or computer-key owner, retrigger that note through the existing non-MIDI hold path (`engine.noteOn` at `PLAYABLE_VELOCITY` without incrementing `noteHoldCount`) so the engine sounds while the UI stays held. Extend the existing multi-source hold describe in `play-surface.spec.ts` with a MIDI+pointer case covering MIDI `allNotesOff` while a non-MIDI owner remains. Add a note-48 emit case that spies `noteOn` and asserts no extra pitch readout element was added.

Do not add a `/settings` route in this plan. Do not persist last device id (D-20 is 12-04).
  </action>

  <verify>
    <automated>npm test -- --include='**/midi-session.spec.ts'</automated>
    <automated>npm test -- --include='**/play-surface.spec.ts'</automated>
  </verify>

  <acceptance_criteria>
    - Named states `'unsupported' | 'permission-denied' | 'no-devices' | 'disconnected' | 'ready'` plus internal `'off'` are each reachable by a named spec (D-05, Pitfall 9).
    - Disconnect-other-device spec: selected id unchanged (D-04).
    - Disconnect-selected spec: `allNotesOff` called and status `'disconnected'` (D-09).
    - Unselected input's `onmidimessage` is `null` after enable.
    - MIDI+pointer C4 hold-count spec passes (D-17).
    - Existing pointer/keyboard `play-surface.spec.ts` cases still pass.
  </acceptance_criteria>

  <done>MIDI state machine, D-04 reattach-or-stay-disconnected, D-09 allNotesOff, D-11 ignore, and D-17 shared hold are proven with fakes.</done>
</task>

</tasks>

<verification>
- Focused MIDI parser, session, and PlaySurface specs pass.
- `npm run build` and `npm run lint` pass before close-out.
- App remains playable with `REQUEST_MIDI_ACCESS` provided as `null`.
</verification>

<success_criteria>
- A fake MIDI keyboard can play and release notes through the shared play surface.
- Enable MIDI is a user gesture, does not steal focus, and starts audio when needed.
- Disconnect and disable cannot leave a stuck voice.
</success_criteria>

<output>
Create `.planning/phases/12-midi-and-patch-persistence/12-02-SUMMARY.md` when done
</output>
