---
phase: 07-audioworklet-dsp-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - tsconfig.worklet.json
  - scripts/build-worklet.mjs
  - worklets/dx7-worklet-processor.ts
  - src/app/domain/dx7/dsp/operator.ts
  - src/app/domain/dx7/dsp/operator.spec.ts
  - src/app/domain/dx7/dsp/worklet-messages.ts
  - src/app/domain/dx7/dsp/worklet-messages.spec.ts
  - src/app/domain/dx7/dsp/additive-fixture.ts
  - src/app/domain/dx7/dsp/additive-fixture.spec.ts
  - src/app/core/audio/worklet-processor-bundle.spec.ts
autonomous: false
requirements: [ENGINE-01]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ENGINE-01 (D-05): a single `PhaseModulatedOperator` at 44100 Hz renders a deterministic 128-sample block whose every sample matches the closed-form `Math.sin(2 * Math.PI * f * i / sampleRate)` reference to 6 decimal places — proven by `npm test` in Node with no browser, no `AudioContext` and no jsdom Web Audio surface."
    - "ENGINE-01 (D-05, Pitfall 4): the same analytical match still holds sample-for-sample across a one-second block (44100 samples), so the phase accumulator's error stays flat instead of growing with sample index on a long held note."
    - "ENGINE-01 (D-03): the operator primitive accepts an optional per-sample phase-modulation input as a first-class parameter — a constant `Math.PI / 2` modulation buffer shifts output to `sin(2*pi*f*t + pi/2)` within the same tolerance, and omitting the argument entirely produces output identical to passing an all-zero buffer."
    - "ENGINE-01 (D-04, D-05): six independent operators at hand-picked fixture frequencies, summed by `AdditiveOperatorBank`, produce output equal sample-for-sample to the ascending-index Float32Array accumulation of the six individually rendered blocks — the additive case sums exactly, with no scaling, smoothing or rounding introduced by the bank."
    - "ENGINE-01 (D-04): the additive fixture's frequencies come from constants declared in `additive-fixture.ts` alone; nothing under `src/app/domain/dx7/dsp/` reads the canonical 32-algorithm dataset or the role-derivation module, so no thin slice of Phase 8's graph-to-kernel translation leaks into this phase."
    - "ENGINE-01 (ROADMAP success criterion 1, part one): `npm run build:worklet` produces `public/worklets/dx7-worklet-processor.js` as a single self-contained script that calls `registerProcessor`, so `audioWorklet.addModule()` has a real, standalone, same-origin URL to fetch."
    - "ENGINE-01: the built worklet bundle, evaluated in Node against stub `AudioWorkletProcessor` / `registerProcessor` / `sampleRate` globals, registers under the processor name the main thread will ask for and fills a 128-sample output channel with the same analytically-correct sine the pure kernel produces — the adapter is covered by an automated deterministic test, not only by human listening."
    - "ENGINE-01 (Pitfall 3): `npm run typecheck:worklet` exits 0 — the adapter typechecks against `@types/audioworklet` in its own TypeScript program, while `npm run build` still typechecks `src/**` with the app's own `lib.dom` and never sees the adapter's worklet-only globals."
    - "T-07-01 mitigation: `parseWorkletMessage` returns `null` for every malformed payload shape — non-object, null, array, unknown kind, non-numeric frequency, `NaN`, `Infinity`, zero, negative, out-of-range, unknown mode — and never throws for any of them, including a payload whose property getter throws, so a bad message can never mutate kernel state or kill the render thread."
    - "ACCEPTANCE_CRITERIA floor: every rendered sample is finite and within [-1, 1] for a single operator, and a modulation buffer containing `NaN` or `Infinity` still yields finite output."
    - "`npm run build`, `npm test` and `npm run lint` are all green with the new kernel, adapter, tsconfig and build script in place."
  prohibitions:
    - "This kernel MUST NOT be described, documented or commented as an exact DX7 emulation or reproduction — it is an original phase-modulation implementation built toward DX7-style behaviour, and every claim about it must stay within what the tests actually prove."
    - "The DSP kernel MUST NOT be derived from, transcribed from, or adapted out of copyrighted or GPLv3 synthesizer source (Dexed in particular) — all math here must be written from first principles or from openly-documented signal-processing identities."
    - "No output path introduced by this plan may be able to exceed the project's established hearing-safety ceiling — the additive bank's unscaled six-carrier sum must stay inside the kernel and never reach a destination node without the attenuation Phase 5 already proved safe."
    - "TypeScript strictness, ESLint rules, bundle budgets and existing tests MUST NOT be disabled, loosened or skipped to make the worklet toolchain compile or the bundle build."
  artifacts:
    - path: src/app/domain/dx7/dsp/operator.ts
      provides: "PhaseModulatedOperator (phase accumulator + optional per-sample PM input), TWO_PI, RENDER_QUANTUM_FRAMES"
      contains: "PhaseModulatedOperator"
    - path: src/app/domain/dx7/dsp/operator.spec.ts
      provides: "Analytical sin(2*pi*f*t) reference match, long-block precision regression, PM-input coverage, finiteness floor"
    - path: src/app/domain/dx7/dsp/additive-fixture.ts
      provides: "ADDITIVE_FIXTURE_BASE_FREQUENCY_HZ, ADDITIVE_FIXTURE_HARMONIC_MULTIPLIERS, ADDITIVE_FIXTURE_FREQUENCIES_HZ, AdditiveOperatorBank"
      contains: "AdditiveOperatorBank"
    - path: src/app/domain/dx7/dsp/additive-fixture.spec.ts
      provides: "Exact per-operator-sum equality proof for the six-carrier additive case"
    - path: src/app/domain/dx7/dsp/worklet-messages.ts
      provides: "DX7_OPERATOR_PROCESSOR_NAME, WorkletRenderMode, WorkletMessage union, parseWorkletMessage, setFrequencyMessage, setModeMessage, frequency bounds"
      contains: "parseWorkletMessage"
    - path: src/app/domain/dx7/dsp/worklet-messages.spec.ts
      provides: "Hostile-payload table proving parseWorkletMessage rejects and never throws (T-07-01)"
    - path: worklets/dx7-worklet-processor.ts
      provides: "Dx7OperatorProcessor — the project's only AudioWorkletProcessor adapter"
      contains: "registerProcessor"
    - path: tsconfig.worklet.json
      provides: "Worklet-only TypeScript program with @types/audioworklet, outside the app's typecheck"
    - path: scripts/build-worklet.mjs
      provides: "esbuild prebuild producing an import-free IIFE bundle in public/worklets/"
    - path: src/app/core/audio/worklet-processor-bundle.spec.ts
      provides: "Evaluates the built bundle in Node against stub worklet globals and asserts registration plus correct rendered output"
  key_links:
    - from: "worklets/dx7-worklet-processor.ts"
      to: "src/app/domain/dx7/dsp/operator.ts"
      via: "relative import across the src boundary — the adapter holds zero DSP math of its own, so everything audible is already covered by the kernel's Vitest suite"
    - from: "scripts/build-worklet.mjs"
      to: "public/worklets/dx7-worklet-processor.js"
      via: "esbuild bundle+iife — the only mechanism that turns the adapter into something audioWorklet.addModule() can fetch; if this link breaks the worklet silently never loads"
    - from: "worklets/dx7-worklet-processor.ts"
      to: "src/app/domain/dx7/dsp/worklet-messages.ts"
      via: "parseWorkletMessage guards every port.onmessage payload before any kernel field is mutated — the single validation choke point for T-07-01"
    - from: "package.json prebuild/prestart/pretest"
      to: "scripts/build-worklet.mjs"
      via: "npm lifecycle hooks — the only defence against Pitfall 6's stale bundle, and what lets the bundle spec assume a fresh artifact exists"
---

<objective>
Build the pure phase-modulation DSP kernel and the worklet toolchain that turns it into a real,
loadable `AudioWorkletProcessor` — the whole vertical path from Angular-free math to a self-contained
browser script, proven end to end without a browser.

Purpose: ENGINE-01's kernel half. ROADMAP success criterion 2 ("DSP kernel is tested with
deterministic sample blocks outside the browser") is fully discharged here, and the build/loading
half of criterion 1 is put in place and automatically verified. Phase 8 (routing/feedback) and
Phase 9 (envelopes) build directly on the operator primitive's shape, which is why D-03's
phase-modulation input port is first-class from the first commit rather than retrofitted.

Output: `src/app/domain/dx7/dsp/` (operator primitive, additive fixture, worklet message contract,
all Vitest-covered), `worklets/dx7-worklet-processor.ts`, `tsconfig.worklet.json`,
`scripts/build-worklet.mjs`, npm lifecycle wiring, and an automated bundle test that evaluates the
built script in Node.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audioworklet-dsp-foundation/07-CONTEXT.md
@.planning/phases/07-audioworklet-dsp-foundation/07-RESEARCH.md
@.planning/phases/07-audioworklet-dsp-foundation/07-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Every symbol and file below is new to this repository — none of it exists before this plan.

**New files (this plan):**
- `src/app/domain/dx7/dsp/operator.ts`
- `src/app/domain/dx7/dsp/operator.spec.ts`
- `src/app/domain/dx7/dsp/additive-fixture.ts`
- `src/app/domain/dx7/dsp/additive-fixture.spec.ts`
- `src/app/domain/dx7/dsp/worklet-messages.ts`
- `src/app/domain/dx7/dsp/worklet-messages.spec.ts`
- `worklets/dx7-worklet-processor.ts`
- `tsconfig.worklet.json`
- `scripts/build-worklet.mjs`
- `src/app/core/audio/worklet-processor-bundle.spec.ts`
- `public/worklets/dx7-worklet-processor.js` (build output, git-ignored)

**New exported symbols (this plan):**
- `TWO_PI`, `RENDER_QUANTUM_FRAMES`, `PhaseModulatedOperator` (class; `setFrequencyHz`, `resetPhase`,
  `render(output, modulationInput?)`)
- `ADDITIVE_FIXTURE_BASE_FREQUENCY_HZ`, `ADDITIVE_FIXTURE_HARMONIC_MULTIPLIERS`,
  `ADDITIVE_FIXTURE_FREQUENCIES_HZ`, `AdditiveOperatorBank` (class; `setBaseFrequencyHz`,
  `render(output)`)
- `DX7_OPERATOR_PROCESSOR_NAME`, `WorkletRenderMode`, `WORKLET_RENDER_MODES`,
  `MAX_OPERATOR_FREQUENCY_HZ`, `SetFrequencyMessage`, `SetModeMessage`, `WorkletMessage`,
  `parseWorkletMessage`, `setFrequencyMessage`, `setModeMessage`

**New npm scripts (this plan):** `build:worklet`, `typecheck:worklet`, `prebuild`, `prestart`,
`pretest`
**New devDependencies (this plan):** `esbuild`, `@types/audioworklet`

**Later plans in this phase add:** `AudioWorkletPortLike`, `AudioWorkletNodeLike`,
`AudioWorkletLike`, `AudioWorkletContextLike`, `AudioWorkletNodeOptionsLike`,
`AudioWorkletNodeConstructorLike`, `supportsAudioWorklet`, `AUDIO_WORKLET_NODE_CTOR`,
`AUDIO_WORKLET_MODULE_URL`, `FakeAudioWorkletPort`, `FakeAudioWorkletNode`, `FakeAudioWorklet`,
`FakeAudioWorkletContext`, `WorkletSynthEngine` (07-02); the dev harness, `tsconfig.harness.json`
and the `harness` npm script (07-03).
</artifacts_this_phase_produces>

<flagged_assumptions>
**The deterministic edge probe could not classify ENGINE-01. The acceptance criteria in
`must_haves.truths` above are planner-authored, not probe-confirmed.**

No `07-SPEC.md` exists, so the spec-less probe fallback ran the deterministic edge probe against
ENGINE-01's raw requirement text. It returned `unclassified` / `unresolved` (1 applicable, 0
resolved, 1 unresolved, 0 explicit, 0 backstop).

| Requirement | Probe category | Probe status | Disposition in this plan |
|-------------|----------------|--------------|--------------------------|
| ENGINE-01 | unclassified | unresolved | Left unresolved, never auto-backstopped. Acceptance criteria were authored by the planner from ROADMAP Phase 7's two success criteria, REQUIREMENTS.md's "testable offline" clause, `docs/ACCEPTANCE_CRITERIA.md`'s "DSP tests render deterministic sample blocks and reject non-finite output" floor, and CONTEXT.md D-03/D-04/D-05. No automated edge-coverage gate stands behind them; the specs in this plan are the only backstop. |

Prohibitions above come from this plan's own Stage-1 adversarial recall / Stage-2 precision pass and
are descriptor-less (`flagged-unverified`) — there is no wired check behind any of them yet.
Canon-referral breadcrumb: non-finite / malformed message payloads reaching the DSP kernel are canon
security, covered by threat row `T-07-01` and `/gsd-secure-phase`, so they are not minted as a
prohibition.

**Second flagged assumption (planner-introduced, not from any probe):** RESEARCH.md Assumption A5
states that `ng serve` picking up a freshly rebuilt `public/worklets/*.js` without a restart is
unverified. This plan mitigates it structurally with `prebuild`/`prestart`/`pretest` lifecycle
hooks, but the "no dev-server restart needed" half stays unproven until the 07-03 listening
checkpoint exercises it in a real browser.
</flagged_assumptions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the esbuild package legitimacy before adding it as an explicit devDependency</name>
  <read_first>
    - .planning/phases/07-audioworklet-dsp-foundation/07-RESEARCH.md (section `## Package Legitimacy Audit` — the full verdict table and the recorded false-positive rationale)
    - package.json (the current devDependencies block — confirm neither package is listed explicitly today)
  </read_first>
  <what-built>
    Nothing yet. This is a pre-install gate. The research pass ran the package legitimacy protocol
    over the two packages this phase needs and returned:

    - `esbuild` — verdict **SUS**, seam reason "too-new". Research judged this a false positive:
      the signal reflects the most recent patch-release date, not package age. Counter-evidence
      recorded in 07-RESEARCH.md: ~255,499,108 weekly downloads, canonical repo
      github.com/evanw/esbuild (~8 years), already resolvable in this repo's own `node_modules` at
      0.28.1 because `@angular/build` depends on it internally. Latest on npm: 0.28.2.
      SPDX license: MIT — record alongside the version/legitimacy check.
    - `@types/audioworklet` — verdict **OK**, published by microsoft/TypeScript-DOM-Lib-Generator
      (the same generator that produces this project's `lib.dom.d.ts`), ~197,097 weekly downloads,
      version 0.0.100. SPDX license: MIT — record alongside the version/legitimacy check.

    Protocol requires a blocking human confirmation before a `[SUS]` package is added as an explicit
    dependency, regardless of the research pass's own reasoning. This checkpoint is not
    auto-approvable.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/esbuild — confirm the repository link points at
       github.com/evanw/esbuild, the weekly download count is in the hundreds of millions, and the
       package is not a recently-created lookalike (check the "Version History" tab shows years of
       releases, not weeks). Confirm SPDX/license metadata is MIT and record it with the version.
    2. Open https://www.npmjs.com/package/@types/audioworklet — confirm the repository link points at
       github.com/microsoft/TypeScript-DOM-Lib-Generator and the publisher is the `types` org.
       Confirm SPDX/license metadata is MIT and record it with the version.
    3. In this repo, run `node -e "console.log(require('esbuild/package.json').version)"` and confirm
       it prints a version — proving esbuild is already installed transitively today, so this is a
       version pin rather than a new attack surface.
    4. Decide whether pinning `esbuild` explicitly is acceptable. If it is not, the alternative is to
       invoke the already-hoisted transitive copy without listing it — say so and the executor will
       adjust Task 2 rather than run `npm install --save-dev esbuild`.
  </how-to-verify>
  <acceptance_criteria>
    - A human has typed an explicit approval or rejection; silence, timeout, or `workflow.auto_advance` does not satisfy this task.
    - On approval, `package.json` devDependencies gains both `esbuild` and `@types/audioworklet` in Task 2.
    - On rejection, the executor records the rejection in the plan SUMMARY and adjusts Task 2 to consume the transitive esbuild copy without adding the dependency.
  </acceptance_criteria>
  <resume-signal>Type "approved" to allow `npm install --save-dev esbuild @types/audioworklet`, or describe the alternative you want.</resume-signal>
  <done>A human has explicitly approved or rejected pinning `esbuild` as a devDependency, and the decision is recorded.</done>
</task>

<task type="tracer">
  <name>Task 2: End-to-end "one sine operator renders through a real worklet bundle" — single path only</name>
  <files>src/app/domain/dx7/dsp/operator.ts, src/app/domain/dx7/dsp/operator.spec.ts, worklets/dx7-worklet-processor.ts, tsconfig.worklet.json, scripts/build-worklet.mjs, package.json, .gitignore, src/app/core/audio/worklet-processor-bundle.spec.ts</files>
  <read_first>
    - src/app/domain/dx7/audio/value-conversion.ts (lines 1-40 — the domain-purity header-comment convention, the terse-doc-per-export convention, and the DOMAIN-04 callout style the new kernel must mirror)
    - src/app/domain/dx7/audio/value-conversion.spec.ts (lines 1-45 — plain Vitest globals, no TestBed, one `describe` per exported symbol, `toBeCloseTo(expected, precision)` idiom)
    - eslint.config.js (the third defineConfig entry, `files: ['src/app/domain/**/*.ts']` — the DOMAIN-04 gate that will fail lint on any Angular import, value or type, inside the new dsp directory)
    - tsconfig.app.json and tsconfig.json (the `include: ["src/**/*.ts"]` glob that is exactly why the adapter must live outside `src/`, and the base compilerOptions the new worklet tsconfig extends)
    - package.json (current scripts block and devDependencies — add to it, do not restructure it)
    - angular.json (the `assets` entry `{"glob": "**/*", "input": "public"}` — the existing rule that will serve the build output verbatim; do NOT modify this file)
    - .planning/phases/07-audioworklet-dsp-foundation/07-RESEARCH.md (`## Architecture Patterns` Pattern 1 and Pattern 2, `## Code Examples`, and Pitfalls 1-5)
    - .planning/phases/07-audioworklet-dsp-foundation/07-PATTERNS.md (the `operator.ts` and `dx7-worklet-processor.ts` pattern assignments)
  </read_first>
  <behavior>
    - A `PhaseModulatedOperator` constructed at `sampleRate` 44100 and `frequencyHz` 440 fills a 128-sample `Float32Array` whose every element equals `Math.sin(2 * Math.PI * 440 * i / 44100)` to 6 decimal places.
    - The same operator rendering 44100 consecutive samples still matches the closed-form reference to 6 decimal places at the final sample, not only near the start.
    - Passing a modulation buffer filled with `Math.PI / 2` produces `Math.sin(2 * Math.PI * f * i / sampleRate + Math.PI / 2)` to 6 decimal places.
    - Omitting the modulation argument produces output identical, element for element, to passing an all-zero buffer of the same length.
    - A modulation buffer containing `NaN` and `Infinity` still yields output where every element satisfies `Number.isFinite` and lies within [-1, 1].
    - Constructing with a non-finite or non-positive `sampleRate`, or calling `setFrequencyHz` with a non-finite or negative value, throws a `RangeError` naming the offending value.
    - `resetPhase()` returns the accumulator to 0 so a re-rendered block equals the first block exactly.
    - The built bundle, evaluated in Node with stub `AudioWorkletProcessor` / `registerProcessor` / `sampleRate` globals, registers exactly one processor under `'dx7-operator'`, and that processor's `process` call fills a 128-sample channel matching the pure kernel's own output for the same frequency.
  </behavior>
  <action>
    Deliver the thinnest complete path from pure math to a browser-loadable script. Single operator,
    single frequency, no message passing, no additive mode — those are Task 3's expansion. Everything
    written here is production code that Task 3 and Phase 8 build on, not a throwaway probe.

    First, per Task 1's approval, run `npm install --save-dev esbuild @types/audioworklet`.

    Create `src/app/domain/dx7/dsp/operator.ts` with a domain-purity header comment in the exact
    style of `value-conversion.ts`, citing DOMAIN-04, ENGINE-01 and D-05 as the reason this module
    is Angular-free. Export `TWO_PI` (`2 * Math.PI`), `RENDER_QUANTUM_FRAMES` (128, documented as the
    Web Audio render-quantum size and the same value `web-audio-synth-engine.ts` already uses for
    `FEEDBACK_DELAY_RENDER_QUANTUM_FRAMES`), and a class `PhaseModulatedOperator` whose constructor
    takes `sampleRate: number` and `frequencyHz: number` as explicit parameters — it must never read
    any global, which is precisely what makes it runnable under Vitest (RESEARCH Anti-Patterns, last
    bullet). Give it a private `phase` field in `[0, 1)`, `setFrequencyHz(frequencyHz: number): void`,
    `resetPhase(): void`, and `render(output: Float32Array, modulationInput?: Float32Array): void`.

    `render` writes exactly `output.length` samples in place and allocates nothing — no freshly
    constructed typed array, no array literal, no object literal, no closure created per call
    (CLAUDE.md's audio rule and RESEARCH Pitfall 5). Per sample: read the modulation value as
    `modulationInput ? modulationInput[i] : 0`, substitute `0` when that value fails
    `Number.isFinite`, write `Math.sin(TWO_PI * phase + modulation)`, then advance the accumulator as
    `phase = (phase + increment) % 1` where `increment = frequencyHz / sampleRate`. The modulo wrap
    on every single sample is load-bearing, not cosmetic — it is what keeps the analytical match flat
    over long blocks instead of drifting (RESEARCH Pitfall 4); hoist `increment` out of the loop.
    Validate in the constructor and in `setFrequencyHz` with `RangeError`s shaped like the
    `validateNote` / `validateVelocity` guards in `web-audio-synth-engine.ts` (message names the
    bound and the received value).

    Create `src/app/domain/dx7/dsp/operator.spec.ts` covering every item in `<behavior>` above, using
    plain Vitest globals and `toBeCloseTo(expected, 6)` — never `TestBed`, never a real
    `AudioContext`. Compute each expected value from the closed form directly in the assertion loop;
    do not precompute it with the same accumulator the implementation uses, or the test proves
    nothing. For the 44100-sample case, assert every sample against the closed form (not a
    subsampled index set), so the long-block precision regression proves the error does not grow
    anywhere in the block.

    Create `worklets/dx7-worklet-processor.ts` — the project's first and only `AudioWorkletProcessor`,
    modelled on RESEARCH Pattern 2. It imports `PhaseModulatedOperator` from
    `../src/app/domain/dx7/dsp/operator` by relative path and holds zero DSP math of its own. Its
    constructor takes `AudioWorkletNodeOptions`, reads `frequencyHz` out of `processorOptions`,
    validates it (invalid → `440`), and constructs one operator using the worklet global
    `sampleRate`. Hostile/malformed `processorOptions.frequencyHz` shapes must be covered by
    tests proving the invalid→440 fallback (never throw, never use an unvalidated value). Its
    `process`
    implementation renders into `outputs[0][0]`, copies that first channel into any additional output
    channels with `set`, and returns `true` unconditionally. Nothing inside `process` may allocate or
    throw. Register it with `registerProcessor` under the literal name `'dx7-operator'`. This file
    must NOT be imported from anywhere under `src/app/` (RESEARCH Pitfall 1) — the only thing that
    reads it is the esbuild script.

    Create `tsconfig.worklet.json` at the repository root (not inside `worklets/` as 07-PATTERNS.md
    sketched — root placement matches the existing `tsconfig.app.json` / `tsconfig.spec.json`
    convention and keeps `extends` and `include` paths flat; record this as a documented deviation in
    the SUMMARY). It extends `./tsconfig.json`, sets `"types": ["audioworklet"]`, `"lib": ["ES2022",
    "DOM"]`, `"noEmit": true`, and `"include": ["worklets/dx7-worklet-processor.ts"]` — one entry
    file only, so the harness added in 07-03 never lands in this program. If `@types/audioworklet`
    collides with `lib.dom` on a duplicate global declaration despite the inherited
    `skipLibCheck: true`, drop `"DOM"` from `lib` and re-run; record whichever combination compiles.

    Create `scripts/build-worklet.mjs` using esbuild's JS API: `entryPoints:
    ['worklets/dx7-worklet-processor.ts']`, `bundle: true`, `format: 'iife'`, `target: 'es2022'`,
    `outfile: 'public/worklets/dx7-worklet-processor.js'`. The IIFE format is what guarantees a
    self-contained script with no runtime module statements, which is required because ES-module
    imports inside `AudioWorkletGlobalScope` are not reliably supported across browsers (RESEARCH
    Anti-Patterns). Keep the script small and side-effect-only; it will grow one flag in 07-03.

    Wire `package.json` scripts: `"build:worklet": "node scripts/build-worklet.mjs"`,
    `"typecheck:worklet": "tsc -p tsconfig.worklet.json"`, and the three npm lifecycle hooks
    `"prebuild"`, `"prestart"` and `"pretest"`, each running `npm run build:worklet`. Those hooks are
    the structural answer to RESEARCH Pitfall 6's stale-bundle failure mode and they are what lets
    the bundle spec below assume a fresh artifact. Add `/public/worklets/` to `.gitignore` — the
    bundle is a deterministic build output with no unique information, and committing it would let a
    stale copy silently diverge from source (RESEARCH Open Question 1).

    Create `src/app/core/audio/worklet-processor-bundle.spec.ts`. This is the automated coverage that
    07-RESEARCH.md Pitfall 2 assumed was impossible: read
    `public/worklets/dx7-worklet-processor.js` from disk with `node:fs`, evaluate it through
    `new Function('AudioWorkletProcessor', 'registerProcessor', 'sampleRate', source)` so those three
    identifiers resolve to test-supplied stubs rather than absent globals, and capture the registered
    name and class. The `AudioWorkletProcessor` stub is a class exposing a `port` object with a
    settable `onmessage` and a `postMessage` no-op. Instantiate the captured class with
    `{ processorOptions: { frequencyHz: 440 } }`, call `process` with an outputs array holding one
    128-length `Float32Array` channel, and assert the result against a `PhaseModulatedOperator`
    rendered identically in the same test. If `node:fs` is not importable from a spec under this
    project's `@angular/build:unit-test` Vitest setup, fall back to a Vite `?raw` import of the same
    path and record which mechanism worked.
  </action>
  <verify>
    <automated>npm run typecheck:worklet && npm run build:worklet && npm test && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:worklet` exits 0.
    - `npm run build:worklet` exits 0 and `public/worklets/dx7-worklet-processor.js` exists afterwards.
    - `grep -c 'registerProcessor' public/worklets/dx7-worklet-processor.js` is at least 1.
    - `node -e "const s=require('fs').readFileSync('public/worklets/dx7-worklet-processor.js','utf8'); process.exit(/^\s*(im|ex)port[\s{*]/m.test(s)||/\brequire\s*\(/.test(s)?1:0)"` exits 0, proving the bundle is self-contained with no module-loading statements.
    - `npm test` exits 0.
    - `operator.spec.ts` contains a named test asserting a 128-sample block matches `Math.sin(2 * Math.PI * f * i / sampleRate)` via `toBeCloseTo(expected, 6)`.
    - `operator.spec.ts` contains a named test rendering 44100 samples and asserting every sample matches the closed form.
    - `operator.spec.ts` contains a named test proving a constant `Math.PI / 2` modulation buffer shifts the output, and a named test proving the omitted argument equals an all-zero buffer.
    - `operator.spec.ts` contains a named test proving a modulation buffer holding `NaN` and `Infinity` yields only finite samples inside [-1, 1].
    - `worklet-processor-bundle.spec.ts` asserts exactly one processor is registered, that its registered name is `'dx7-operator'`, and that its rendered 128-sample output matches a directly-constructed `PhaseModulatedOperator` at the same frequency.
    - Invalid/hostile `processorOptions.frequencyHz` values fall back to `440` (no throw); tests cover that fallback.
    - `grep -v '^\s*\*' src/app/domain/dx7/dsp/operator.ts | grep -v '^\s*//' | grep -c 'new Float32Array'` is 0 — no allocation in the render loop, counting code only, not doc comments.
    - `grep -rln "worklets/dx7-worklet-processor" src/app --include=*.ts | wc -l` is 1 (the bundle spec's path string only) — no Angular source file imports the adapter.
    - `npm run build` exits 0 and `npm run lint` exits 0.
    - `git diff --exit-code angular.json` exits 0 — the asset pipeline was not modified.
  </acceptance_criteria>
  <reversibility rating="costly">The `PhaseModulatedOperator` constructor/`render` signature — including D-03's optional per-sample modulation port — is what Phase 8's graph traversal will be written against; changing its shape later means reworking the primitive after other code depends on it, not just adding a routing layer (CONTEXT.md D-03).</reversibility>
  <done>One sine operator's output is analytically proven in Node, bundled into a standalone browser script, and that script is proven to register and render correctly when evaluated — the whole path exists and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand to the additive six-operator case and a validated worklet message contract</name>
  <files>src/app/domain/dx7/dsp/additive-fixture.ts, src/app/domain/dx7/dsp/additive-fixture.spec.ts, src/app/domain/dx7/dsp/worklet-messages.ts, src/app/domain/dx7/dsp/worklet-messages.spec.ts, worklets/dx7-worklet-processor.ts, src/app/core/audio/worklet-processor-bundle.spec.ts</files>
  <read_first>
    - src/app/domain/dx7/dsp/operator.ts (Task 2's primitive — the exact constructor and `render` signature the bank composes)
    - src/app/domain/dx7/dsp/operator.spec.ts (Task 2's spec conventions — match them)
    - worklets/dx7-worklet-processor.ts (Task 2's adapter — extend it, do not rewrite it)
    - src/app/core/audio/worklet-processor-bundle.spec.ts (Task 2's bundle harness — extend it)
    - src/app/domain/dx7/models/operator.ts (`OperatorId` / `OPERATOR_IDS` — the six-operator restricted-union convention the fixture's arity mirrors)
    - src/app/core/audio/web-audio-synth-engine.ts (lines 78-90 — `validateNote` / `validateVelocity`, the validation-before-mutation shape `parseWorkletMessage` mirrors in a non-throwing form)
    - .planning/phases/07-audioworklet-dsp-foundation/07-RESEARCH.md (`## Security Domain` — the V5 input-validation control and the malformed-postMessage threat pattern this task closes)
  </read_first>
  <behavior>
    - `AdditiveOperatorBank` at 44100 Hz, block size 128, using the fixture frequencies, renders output equal element-for-element to a `Float32Array` accumulated as `sum[i] += operatorOutput[i]` over the six operators in ascending index order.
    - Calling `render` with a buffer whose length differs from the configured block size throws a `RangeError`.
    - `setBaseFrequencyHz(base)` sets operator `n` to `base * ADDITIVE_FIXTURE_HARMONIC_MULTIPLIERS[n]`, and a subsequent render matches six independently-constructed operators at those same frequencies.
    - `AdditiveOperatorBank` allocates its scratch buffer once in the constructor; repeated `render` calls allocate nothing.
    - `parseWorkletMessage` returns a typed message for `{ kind: 'setFrequency', frequencyHz: 440 }` and for `{ kind: 'setMode', mode: 'additive' }`.
    - `parseWorkletMessage` returns `null` for: `undefined`, `null`, a number, a string, an array, `{}`, an unknown `kind`, `{ kind: 'setFrequency' }` with a missing / string / `NaN` / `Infinity` / `0` / negative / above-`MAX_OPERATOR_FREQUENCY_HZ` frequency, and `{ kind: 'setMode', mode: 'sideways' }`.
    - `parseWorkletMessage` never throws — including for a payload whose `kind` getter throws.
    - The built bundle's processor, sent a valid `setMode` `'additive'` message followed by a `setFrequency` message, renders the additive sum; sent a malformed payload, it renders unchanged output and throws nothing.
  </behavior>
  <action>
    Expand outward from Task 2's proven slice. Do not restructure anything Task 2 committed.

    Create `src/app/domain/dx7/dsp/worklet-messages.ts` — the one shared contract between the main
    thread, the adapter, and (in 07-03) the dev harness, so none of the three can drift. Same
    domain-purity header convention. Export `DX7_OPERATOR_PROCESSOR_NAME` as `'dx7-operator'` and
    have Task 2's adapter import it instead of repeating the string literal. Export a
    `WorkletRenderMode` union of `'single'` and `'additive'` plus a `WORKLET_RENDER_MODES` readonly
    array (mirroring the `OperatorId` / `OPERATOR_IDS` convention in
    `src/app/domain/dx7/models/operator.ts`), `MAX_OPERATOR_FREQUENCY_HZ` (20000, documented as an
    audible-range guard rather than a Nyquist claim), readonly interfaces `SetFrequencyMessage` and
    `SetModeMessage`, their `WorkletMessage` union, the two builder functions `setFrequencyMessage`
    and `setModeMessage`, and `parseWorkletMessage(data: unknown): WorkletMessage | null`.

    `parseWorkletMessage` is this phase's single security choke point (threat `T-07-01`): it is the
    only thing standing between an arbitrary structured-clone payload and the phase accumulator. It
    must narrow `unknown` without any type assertion that skips a runtime check, reject anything
    whose numeric field fails `Number.isFinite` or falls outside `(0, MAX_OPERATOR_FREQUENCY_HZ]`,
    reject any `mode` not in `WORKLET_RENDER_MODES`, and return `null` rather than throwing for every
    rejection. Wrap the whole body in a try/catch returning `null`, so even a payload with a throwing
    property getter cannot escape as an exception into the render thread's message handler.

    Create `src/app/domain/dx7/dsp/additive-fixture.ts` implementing D-04's synthetic proof case.
    Export `ADDITIVE_FIXTURE_BASE_FREQUENCY_HZ` (220), `ADDITIVE_FIXTURE_HARMONIC_MULTIPLIERS`
    (a readonly six-element array `[1, 2, 3, 4, 5, 6]`), `ADDITIVE_FIXTURE_FREQUENCIES_HZ` derived
    from those two, and a class `AdditiveOperatorBank` with constructor
    `(sampleRate: number, blockSize: number, frequencies?: readonly number[])`. Document in the
    header that these frequencies are hand-picked and that this module deliberately does not read
    `algorithms.ts` or `derive-role.ts` — graph-to-kernel translation is Phase 8's job (D-04), and
    hardcoding "algorithm 32 means additive" here would be exactly the duplicated routing knowledge
    CLAUDE.md's domain rules forbid.

    The bank owns one `PhaseModulatedOperator` per frequency and exactly one scratch `Float32Array`
    of `blockSize`, both allocated in the constructor. `render(output)` throws a `RangeError` when
    `output.length` differs from `blockSize`, then does `output.fill(0)` and, for each operator in
    ascending index order, renders into the scratch buffer and accumulates with
    `output[i] += scratch[i]`. That accumulation order is part of the contract, not an implementation
    detail: `additive-fixture.spec.ts` proves the exact-sum property by accumulating six
    independently-rendered blocks into a `Float32Array` in the same ascending order and asserting
    element-for-element equality with `toBe`, which only holds because both sides round through
    float32 after each addition in the same sequence. Also expose
    `setBaseFrequencyHz(baseHz: number): void` which retunes operator `n` to
    `baseHz * ADDITIVE_FIXTURE_HARMONIC_MULTIPLIERS[n]` so 07-03's harness can play notes in additive
    mode. The bank applies no gain and no scaling — the unscaled six-carrier sum stays inside the
    kernel, and attenuation is the engine's job (plan 07-02 reuses Phase 5's already-proven
    `MASTER_GAIN` safety clamp for that).

    Write `worklet-messages.spec.ts` and `additive-fixture.spec.ts` first, red, covering every
    `<behavior>` item. Use a `describe.each` / table-driven shape for the hostile-payload matrix so a
    new rejected shape is one table row, mirroring how `lessons.spec.ts` iterates its dataset.

    Then extend `worklets/dx7-worklet-processor.ts`: import `AdditiveOperatorBank`,
    `parseWorkletMessage` and `DX7_OPERATOR_PROCESSOR_NAME`; hold both a single operator and a bank;
    keep a `mode` field defaulting to `'single'`; set `this.port.onmessage` in the constructor to a
    handler that calls `parseWorkletMessage` on `event.data` and applies the result, ignoring `null`
    entirely. Construct the bank once up front as a fixed pre-render bank (constructor /
    initialization path only — never allocate or rebuild inside `process`), sized to the
    expected render quantum. `process` selects operator or bank by `mode`, and still never
    allocates or throws.

    Finally extend `worklet-processor-bundle.spec.ts` with the two message cases from `<behavior>`:
    drive the captured processor's `port.onmessage` directly with a valid payload and with a
    malformed one, and assert the rendered output changed in the first case and stayed byte-identical
    with no thrown error in the second.
  </action>
  <verify>
    <automated>npm run typecheck:worklet && npm run build:worklet && npm test && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - `additive-fixture.spec.ts` contains a named test asserting the bank's output equals the ascending-order Float32Array accumulation of six independently-rendered operator blocks, using exact `toBe` equality per element.
    - `additive-fixture.spec.ts` contains a named test asserting a mismatched output length throws `RangeError`.
    - `additive-fixture.spec.ts` contains a named test asserting `setBaseFrequencyHz` retunes each operator to `base * multiplier`.
    - `worklet-messages.spec.ts` covers at least 12 distinct rejected payload shapes, each asserting `parseWorkletMessage` returns `null` and throws nothing, including a payload whose property getter throws.
    - `worklet-processor-bundle.spec.ts` asserts a valid `setMode`/`setFrequency` pair changes rendered output, and that a malformed payload leaves output unchanged with no thrown error.
    - `grep -rhE "^\s*import .* from " src/app/domain/dx7/dsp/*.ts | grep -cE "algorithms|derive-role"` is 0 — no import statement in the dsp module reaches the canonical dataset or role derivation (D-04); the gate reads import lines only, so the header comment explaining the boundary cannot invalidate it.
    - `grep -c 'DX7_OPERATOR_PROCESSOR_NAME' worklets/dx7-worklet-processor.ts` is at least 2 (the import and the registration call) — the adapter takes the processor name from the shared contract module instead of repeating a string literal.
    - `npm run typecheck:worklet`, `npm run build:worklet`, `npm run build` and `npm run lint` all exit 0.
  </acceptance_criteria>
  <reversibility rating="costly">The `WorkletMessage` union and `parseWorkletMessage` fix the main-thread-to-render-thread parameter shape that plan 07-02's engine and 07-03's harness both encode against. RESEARCH Assumption A3 judges the risk low — if Phase 8/9 need `AudioParam`-based audio-rate automation instead, only this contract and the adapter change, never the pure kernel — so this is costly to revisit, not one-way.</reversibility>
  <done>The additive six-carrier case sums exactly and is proven against per-operator output, every malformed worklet message is proven to be rejected without throwing, and the built bundle is proven to honour both.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main thread → `AudioWorkletGlobalScope` | `processorOptions` at construction and `port.postMessage` afterwards are the only channels into the real-time render thread. Both cross a realm boundary with no shared memory and no type checking at runtime — whatever arrives is `unknown`. |
| Static origin → `audioWorklet.addModule()` | The worklet script is fetched by URL and evaluated as code in a privileged real-time realm. Whatever that URL resolves to becomes executing code. |
| npm registry → build toolchain | `esbuild` runs as part of `prebuild`/`pretest` and writes the script that will be evaluated in the render thread. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Denial of Service | `worklets/dx7-worklet-processor.ts` `port.onmessage` | medium | mitigate | Every inbound payload passes through `parseWorkletMessage` before any kernel field is mutated; it narrows `unknown` with runtime checks only, rejects non-finite and out-of-range numerics and unknown modes by returning `null`, and cannot throw (whole body wrapped, covering throwing property getters). A rejected message is a no-op, matching `docs/ACCEPTANCE_CRITERIA.md`'s "reject non-finite output" floor. Proven by the hostile-payload table in `worklet-messages.spec.ts` and the malformed-payload case in `worklet-processor-bundle.spec.ts` (Task 3). |
| T-07-02 | Denial of Service | `PhaseModulatedOperator.render` | medium | mitigate | Defence in depth behind T-07-01: the render loop substitutes `0` for any modulation sample failing `Number.isFinite`, so even a modulation buffer poisoned upstream cannot emit `NaN`/`Infinity` into the audio graph; the accumulator is wrapped modulo 1 every sample so it cannot drift out of range on a long-held note. Proven by the finiteness and long-block tests in `operator.spec.ts` (Task 2). |
| T-07-03 | Denial of Service | `process()` render quantum | medium | mitigate | `process` allocates nothing (all buffers and the additive bank are fixed pre-render allocations; no bank construct/rebuild inside `process`), never throws, and returns `true` unconditionally so the browser keeps calling it. A throw inside `process` permanently stops that node's audio. Enforced by the no-allocation grep gate and the bundle spec's direct `process` invocation (Tasks 2 and 3). |
| T-07-04 | Tampering | `public/worklets/dx7-worklet-processor.js` | low | mitigate | The bundle is a git-ignored deterministic build output regenerated by `prebuild`/`prestart`/`pretest`, never a committed artifact that could silently diverge from source or be edited in place unnoticed. The load URL is a build-time-fixed, same-origin path with no runtime or user-supplied component, so there is no origin to redirect. |
| T-07-SC | Tampering | npm installs (`esbuild`, `@types/audioworklet`) | high | mitigate | `esbuild` carries a `[SUS]` "too-new" verdict from the legitimacy audit (assessed a false positive on 255M weekly downloads and an 8-year canonical repo, and already resolvable transitively in this repo). Task 1 is a non-auto-approvable `blocking-human` checkpoint requiring registry verification of both packages before any install runs. `esbuild` writes the code that executes in the real-time render thread, which is what makes this high rather than medium. |

</threat_model>

<verification>
- `npm run typecheck:worklet`
- `npm run build:worklet`
- `npm test`
- `npm run build`
- `npm run lint`
- `git diff --exit-code angular.json src/app/core/audio/web-audio-synth-engine.ts src/app/core/audio/synth-engine.token.ts`
</verification>

<success_criteria>
- A single operator's rendered block matches the closed-form `sin(2*pi*f*t)` reference to 6 decimal
  places at 44100 Hz, over both a 128-sample block and a 44100-sample block, in Node with no browser.
- The optional per-sample phase-modulation input exists as a first-class parameter and is exercised.
- Six operators summed by the additive bank equal the per-operator sum exactly.
- `npm run build:worklet` emits a self-contained, module-statement-free script that registers a
  processor, and an automated test evaluates that script and proves it renders correctly.
- Every malformed worklet message is rejected without throwing.
- Nothing under `src/app/domain/dx7/dsp/` imports Angular or the canonical algorithm dataset.
- `npm run build`, `npm test` and `npm run lint` are all green; `angular.json` and Phase 5's live
  engine are untouched.
</success_criteria>

<output>
Create `.planning/phases/07-audioworklet-dsp-foundation/07-01-SUMMARY.md` when done.
</output>
