---
phase: 07-audioworklet-dsp-foundation
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - worklets/harness/harness-main.ts
  - worklets/harness/index.html
  - tsconfig.harness.json
  - scripts/build-worklet.mjs
  - package.json
  - .gitignore
  - README.md
autonomous: false
requirements: [ENGINE-01]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ENGINE-01 (D-06): a dev-only harness exists that loads the built worklet in a real browser through `audioWorklet.addModule()` and lets a human trigger both proof cases by ear — the single-operator case and the additive six-carrier case — which no Vitest or jsdom test can do, because jsdom implements no Web Audio API and no `AudioWorkletGlobalScope` at all."
    - "ENGINE-01 (D-06): the harness starts audio only from an explicit user gesture — no `AudioContext` is constructed on page load — matching CLAUDE.md's audio rule and Phase 5's gesture-gate precedent."
    - "ENGINE-01 (D-06): the harness encodes against the same shared worklet message contract and the same pitch and safety-gain conversions the engine uses, so what a human hears cannot drift from what the engine would send."
    - "ENGINE-01 (D-01): the harness ships nothing to production on a clean checkout — its outputs are git-ignored build artifacts emitted only by an explicit harness command, so a clean checkout's `npm run build` produces a dist tree with no dev directory in it, while the worklet bundle itself is still emitted normally. (Structural isolation against the realistic harness-then-build sequence is superseded by 07-04, which relocates harness output outside `public/` and adds fail-closed postbuild / `verify:harness-isolation` gates.)"
    - "ENGINE-01 (RESEARCH Pitfall 6): the stale-bundle failure mode is closed structurally by the npm lifecycle hooks and documented in the README, so a kernel edit cannot silently produce old sound in the harness."
    - "ENGINE-01 (ROADMAP success criterion 1): a human has confirmed, in a real browser, that the worklet loads without console errors and that both the single-operator and additive multi-operator cases sound correct — clean, click-free, at a comfortable level, with no dropouts."
    - "ENGINE-01 (D-07): the phase cannot close without that human confirmation, because Phase 8's routing work and Phase 9's envelope work both build directly on this kernel being trustworthy."
    - "`npm run build`, `npm test`, `npm run lint` and `npm run typecheck:worklet` are all green with the harness in the tree."
  prohibitions:
    - "The harness MUST NOT be described, documented or labelled as an exact DX7 emulation or reproduction — its page copy must say plainly what it is, a development test tone for a phase-modulation kernel."
    - "The harness MUST NOT be derived from, transcribed from, or adapted out of copyrighted or GPLv3 synthesizer source (Dexed in particular)."
    - "The harness MUST NOT be able to produce output above the project's established hearing-safety ceiling — the same clamp Phase 5 proved safe applies before its output reaches the destination, and no control may bypass it."
    - "TypeScript strictness, ESLint rules, bundle budgets and existing tests MUST NOT be disabled, loosened or skipped to make the harness build."
    - "The harness MUST NOT become a shipped product surface — no route, no navigation link, no entry in the Angular app's import graph."
  artifacts:
    - path: worklets/harness/harness-main.ts
      provides: "Standalone dev harness entry point — gesture-gated AudioContext, addModule, worklet node, gain graph, mode and note controls"
      contains: "addModule"
    - path: worklets/harness/index.html
      provides: "Dev harness page with labelled controls and a visible non-shipped banner"
    - path: tsconfig.harness.json
      provides: "Harness-only TypeScript program (DOM types, no worklet globals), separate from the app and the worklet programs"
    - path: scripts/build-worklet.mjs
      provides: "Extended with an opt-in harness flag that additionally emits the harness bundle and page"
    - path: README.md
      provides: "Worklet build and dev harness documentation including the stale-bundle warning"
  key_links:
    - from: "worklets/harness/harness-main.ts"
      to: "src/app/domain/dx7/dsp/worklet-messages.ts"
      via: "the shared processor name and message builders — the harness proves the same contract the engine sends, so a human's ears and the engine's messages cannot diverge"
    - from: "worklets/harness/harness-main.ts"
      to: "public/worklets/dx7-worklet-processor.js"
      via: "audioWorklet.addModule() against the same same-origin path the engine's token defaults to — this is the only place in the phase where that URL is proven to actually resolve and evaluate"
    - from: "scripts/build-worklet.mjs harness flag"
      to: "public/dev/"
      via: "opt-in only — the default invocation used by prebuild/prestart/pretest never writes here, which is what keeps the harness out of production output"
---

<objective>
Build the dev-only listening harness and gate the phase behind a human actually hearing the worklet
run in a real browser.

Purpose: D-06 and D-07. Everything else in this phase is proven in Node against fakes and stubs;
`AudioWorkletGlobalScope` has no polyfill and no jsdom implementation, so the single claim the
ROADMAP makes first — "worklet loads and runs a single operator and an additive multi-operator case
correctly" — is only fully discharged by a human in a browser. Phase 5 and Phase 6 both set this
precedent with blocking listening checkpoints, and Phases 8 and 9 build directly on this kernel, so
it has to be trustworthy before it is extended.

Output: `worklets/harness/` (a standalone bundle outside the Angular app's import graph), a harness
tsconfig, an opt-in build flag, README documentation, and an approved blocking human verification.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audioworklet-dsp-foundation/07-CONTEXT.md
@.planning/phases/07-audioworklet-dsp-foundation/07-01-SUMMARY.md
@.planning/phases/07-audioworklet-dsp-foundation/07-02-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
**New files (this plan):**
- `worklets/harness/harness-main.ts`
- `worklets/harness/index.html`
- `tsconfig.harness.json`
- `public/dev/worklet-harness.js` and `public/dev/worklet-harness.html` (build outputs, git-ignored,
  emitted only by the opt-in harness command)

**Modified files (this plan):** `scripts/build-worklet.mjs` (adds the opt-in harness flag),
`package.json` (adds the `harness` script, extends `typecheck:worklet`), `.gitignore` (ignores the
harness output directory), `README.md` (adds a worklet build and dev harness section).

**New npm script (this plan):** `harness`

**Consumed from earlier plans in this phase:** `DX7_OPERATOR_PROCESSOR_NAME`, `setFrequencyMessage`,
`setModeMessage`, `WorkletRenderMode` (07-01); `DEFAULT_WORKLET_MODULE_URL` (07-02).
**Pre-existing repository surface this plan reuses, unmodified:** `midiNoteToFrequency`,
`MASTER_GAIN`, `velocityToAmplitude` from `src/app/domain/dx7/audio/value-conversion.ts`.
</artifacts_this_phase_produces>

<flagged_assumptions>
**The deterministic edge probe could not classify ENGINE-01.** No `07-SPEC.md` exists; the spec-less
fallback's edge probe returned `unclassified` / `unresolved` for ENGINE-01 (1 applicable, 0 resolved,
0 explicit, 0 backstop). It stays unresolved and is not auto-backstopped. The truths above are
planner-authored from ROADMAP Phase 7's success criterion 1 and CONTEXT.md D-06/D-07. For this plan
specifically there is no automated backstop at all by design — the checkpoint is the verification.

Prohibitions are carried forward from plan 07-01's recall pass plus one harness-specific survivor
(no shipped product surface), all descriptor-less (`flagged-unverified`). Canon-referral breadcrumb:
malformed message payloads reaching the render thread are canon security, covered by `T-07-01`; not
minted as prohibitions.

**Planner-introduced flagged assumptions carried into the checkpoint:**
1. RESEARCH Assumption A5 — whether `ng serve` picks up a freshly rebuilt `public/` artifact without
   a dev-server restart is unverified. The checkpoint script exercises this directly and the answer
   gets recorded in the SUMMARY either way.
2. Per D-01 the harness is standalone and cannot import an Angular `@Injectable`, so
   `WorkletSynthEngine` itself is never executed in a real browser this phase. This is a known,
   accepted gap, restated in the checkpoint so the approving human is not misled about what was
   heard: the ears verify the kernel, the adapter, the bundle and the module-loading path — not the
   Angular engine class, which is covered by fakes in 07-02 and by whichever phase performs the live
   cutover.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Build the standalone dev listening harness behind an opt-in build flag</name>
  <files>worklets/harness/harness-main.ts, worklets/harness/index.html, tsconfig.harness.json, scripts/build-worklet.mjs, package.json, .gitignore, README.md</files>
  <read_first>
    - scripts/build-worklet.mjs (07-01's esbuild script — extend it with a flag, do not restructure it)
    - package.json (the scripts block as 07-01 left it, including the prebuild/prestart/pretest hooks the harness must NOT be added to)
    - tsconfig.worklet.json (07-01's worklet program — the harness config is its sibling, with DOM types instead of worklet globals)
    - src/app/domain/dx7/dsp/worklet-messages.ts (07-01's contract — the exact processor name constant and message builder signatures)
    - src/app/domain/dx7/audio/value-conversion.ts (`midiNoteToFrequency`, `velocityToAmplitude`, `MASTER_GAIN` and the safety-clamp rationale in its doc comment)
    - src/app/core/audio/audio-worklet-node.token.ts (07-02's `DEFAULT_WORKLET_MODULE_URL` — the harness must load the same path the engine's token defaults to)
    - src/app/core/audio/worklet-synth-engine.ts (07-02's `initialize()` build sequence and note-on/off gain scheduling — the harness performs the same steps in plain TypeScript)
    - .gitignore (07-01 added the worklet bundle output; add the harness output directory alongside it)
    - README.md (the `## Verification commands` and `## Architecture summary` sections — the new section goes between them and must match the surrounding tone)
    - CLAUDE.md (the Audio rules and the UI and accessibility rules — the harness page still needs semantic HTML, labelled controls and keyboard operation)
  </read_first>
  <action>
    Create `worklets/harness/harness-main.ts`, a plain TypeScript entry point with no Angular import
    of any kind. It imports `DX7_OPERATOR_PROCESSOR_NAME`, `setFrequencyMessage` and
    `setModeMessage` from `../../src/app/domain/dx7/dsp/worklet-messages`, and `midiNoteToFrequency`,
    `velocityToAmplitude` and `MASTER_GAIN` from `../../src/app/domain/dx7/audio/value-conversion`.
    Importing the shared contract rather than retyping string literals is the whole point of the file:
    a human's ears then verify exactly the payload shape 07-02's engine sends. Do not import
    `WorkletSynthEngine` — it is an Angular `@Injectable` and pulling it in would drag the framework
    runtime into a non-Angular bundle and breach D-01's isolation.

    The harness holds no module-level `AudioContext`. On the enable button's click handler — and only
    there — it constructs the context, awaits `resume()`, awaits
    `audioWorklet.addModule()` against the same same-origin path 07-02's
    `DEFAULT_WORKLET_MODULE_URL` names, constructs an `AudioWorkletNode` under
    `DX7_OPERATOR_PROCESSOR_NAME` with a single output channel, and connects it through a voice gain
    and a master gain to the destination. Schedule the master gain to `MASTER_GAIN` and the voice gain
    to 0 with `setValueAtTime`, exactly as the engine does, so the additive case's unscaled
    six-carrier sum is attenuated by the clamp Phase 5 already proved safe.     Wrap the whole sequence in
    a try/catch that writes the failure into a visible status element and to the console — a silent
    failure here would waste the human checkpoint's time.

    Give the harness a `dispose` path that disconnects the `AudioWorkletNode`, the voice gain, and
    the master gain, then closes the `AudioContext` and nulls those fields. Invoke it on `pagehide`
    and before constructing a new context on a second Enable click, so a reload or re-enable cannot
    leak nodes. Do not replace Stop with dispose — Stop keeps its existing scheduled release toward
    zero; dispose is graph teardown only.

    Give it three actions beyond enable: play the single-operator case, play the additive
    six-carrier case, and stop. Each play action posts a `setModeMessage` for its mode followed by a
    `setFrequencyMessage` built from `midiNoteToFrequency` of a fixed MIDI note (use A4 for the single
    case and A3 for the additive case so the fixture's harmonic stack sits in a comfortable range),
    then ramps the voice gain up over roughly fifteen milliseconds with `linearRampToValueAtTime`
    toward `velocityToAmplitude` of a fixed mid velocity. Stop schedules an exponential release toward
    zero. Use scheduled automation only, never a direct gain assignment — the same click-free rule the
    rest of the project follows, and the thing the human is listening for. Keep a live status line
    reporting the context state, the loaded module URL, and the last posted message.

    Create `worklets/harness/index.html`: semantic HTML with a heading, a prominent banner stating
    plainly that this is a development test page for a phase-modulation DSP kernel and is not part of
    the shipped application, four labelled `button` elements matching the actions above, a status
    region with `aria-live` set to polite, and a `script` tag loading `./worklet-harness.js`. Every
    control must be reachable and operable by keyboard with a visible focus ring, and nothing may
    convey state by colour alone — this is a dev page, but CLAUDE.md's accessibility rules do not have
    an exemption clause, and a human is about to operate it. Do not describe the output as a DX7
    emulation anywhere in the page copy.

    Create `tsconfig.harness.json` at the repository root as a sibling of `tsconfig.worklet.json`. It
    extends `./tsconfig.json`, sets `"types": []`, `"noEmit": true` and
    `"include": ["worklets/harness/**/*.ts"]`. Keeping the harness in its own program is deliberate:
    it needs DOM globals and must not see the worklet-only globals, and the worklet program must not
    see the DOM-only harness — separating them removes any chance of a duplicate-global collision
    between `@types/audioworklet` and `lib.dom`.

    Extend `scripts/build-worklet.mjs` with an opt-in flag read from `process.argv`. Without the flag
    it behaves exactly as 07-01 left it, emitting only the processor bundle. With the flag it
    additionally bundles `worklets/harness/harness-main.ts` to `public/dev/worklet-harness.js` using
    the same esbuild options, and copies `worklets/harness/index.html` to
    `public/dev/worklet-harness.html`. The default-off behaviour is what keeps the harness out of
    production output: `prebuild`, `prestart` and `pretest` all invoke the flagless form, so a clean
    checkout's `npm run build` never creates `public/dev/` at all and therefore never copies it into
    dist. Do not add the harness to any of those lifecycle hooks, and do not modify `angular.json` —
    the existing asset rule needs no change for this to hold.

    Add the `harness` script to `package.json` invoking the flagged form, and extend
    `typecheck:worklet` so it typechecks both worklet and harness programs. Add the harness output
    directory to `.gitignore` next to 07-01's bundle entry.

    Add a `## Worklet build and dev harness` section to `README.md` between the existing
    `## Verification commands` and `## Architecture summary` sections. Document: what the worklet
    bundle is and where it is served from; that `prebuild`, `prestart` and `pretest` regenerate it
    automatically so it cannot go stale (RESEARCH Pitfall 6); how to run the harness — build it, start
    the dev server, open the page — and that if a kernel edit does not change what the harness sounds
    like, rebuild and restart the dev server before assuming the change did nothing (RESEARCH
    Assumption A5, still unverified); and that the harness is a development tool that is never part of
    a production build. Match the surrounding sections' tone and keep the unofficial/educational
    framing intact.
  </action>
  <verify>
    <automated>npm run typecheck:worklet && npm run harness && test -f public/dev/worklet-harness.js && test -f public/dev/worklet-harness.html && rm -rf public/dev && npm run build:worklet && test ! -d public/dev && npm test && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:worklet` exits 0 and typechecks both the worklet and the harness programs.
    - `npm run harness` exits 0 and afterwards both `public/dev/worklet-harness.js` and `public/dev/worklet-harness.html` exist.
    - After removing `public/dev` and running `npm run build:worklet` alone, `test ! -d public/dev` succeeds while `test -f public/worklets/dx7-worklet-processor.js` also succeeds — the harness is opt-in and the processor bundle is not.
    - `npm run build` exits 0 and the resulting dist browser directory contains `worklets/dx7-worklet-processor.js` but no `dev` directory.
    - `grep -c '@angular' worklets/harness/harness-main.ts` is 0 — the harness pulls in no framework code.
    - `grep -c 'DX7_OPERATOR_PROCESSOR_NAME' worklets/harness/harness-main.ts` is at least 1 and `grep -cE 'setFrequencyMessage|setModeMessage' worklets/harness/harness-main.ts` is at least 2 — the harness uses the shared contract rather than hand-built payloads.
    - `grep -c 'MASTER_GAIN' worklets/harness/harness-main.ts` is at least 1 — the safety clamp is applied.
    - `worklets/harness/index.html` contains at least four `button` elements, an `aria-live` status region, and a visible banner stating the page is not part of the shipped application.
    - `.gitignore` contains an entry covering `public/dev`.
    - `README.md` contains a `## Worklet build and dev harness` section naming `build:worklet`, `harness`, and the stale-bundle rebuild guidance.
    - `npm test` exits 0, `npm run lint` exits 0.
    - `git diff --exit-code angular.json src/` exits 0 — no Angular source or build configuration changed.
  </acceptance_criteria>
  <reversibility rating="reversible">The harness is an opt-in, git-ignored dev artifact with no consumers; deleting the directory, the flag and the script removes it completely with no effect on the app, the kernel or the engine. Production-isolation here is the clean-checkout property (flagless lifecycle hooks never emit `public/dev/`); a harness run followed by a production build is out of this plan's scope and is the realistic guarantee 07-04 adds.</reversibility>
  <done>A standalone, accessible dev harness builds on demand, loads the real worklet bundle in a browser through the same URL and message contract the engine uses, applies the proven safety clamp, and — on a clean checkout — contributes nothing to a production build. The harness-then-production-build sequence is guaranteed by 07-04, not by this plan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Listen to the worklet in a real browser and approve the phase (D-07)</name>
  <read_first>
    - README.md (the `## Worklet build and dev harness` section Task 1 added — follow its own instructions, and treat any step that does not work as written as a finding)
    - .planning/phases/07-audioworklet-dsp-foundation/07-CONTEXT.md (D-05, D-06, D-07 — what this checkpoint is required to establish)
    - .planning/phases/07-audioworklet-dsp-foundation/07-01-SUMMARY.md and 07-02-SUMMARY.md (what was already proven automatically, so the listening pass targets only what tests cannot reach)
  </read_first>
  <what-built>
    A pure six-operator-capable phase-modulation DSP kernel, a real `AudioWorkletProcessor` around it,
    an esbuild step that turns it into a standalone browser script, a `SynthEngine` implementation
    over an `AudioWorkletNode`, and a dev-only harness page.

    Already proven automatically, without a browser: the single operator matches the closed-form
    `sin(2*pi*f*t)` reference to six decimal places over both a short and a one-second block; the
    optional phase-modulation input shifts the waveform as expected; six operators sum exactly to
    their per-operator sum; every malformed worklet message is rejected without throwing; the built
    bundle registers and renders correctly when evaluated in Node against stub worklet globals; and
    the engine's lifecycle, gesture gate, load-failure path, message contract, validation and teardown
    all pass against hand-rolled fakes.

    Not provable without a browser, which is why you are here: that
    `audioWorklet.addModule()` actually fetches and evaluates the bundle inside a real
    `AudioWorkletGlobalScope`, that the processor registers there, and that what comes out of the
    speakers is a clean, correct, comfortably-loud tone. jsdom implements no Web Audio API and no
    worklet realm at all, so nothing in `npm test` reaches any of this.

    Known limitation, stated so it does not mislead you: per D-01 the harness is a standalone bundle
    and cannot import the Angular `WorkletSynthEngine` class, so that class itself does not execute in
    this test. Your ears verify the kernel, the adapter, the bundle and the module-loading path. The
    engine class is covered by fakes and will be re-verified in a browser by whichever phase performs
    the live `SYNTH_ENGINE` cutover.
  </what-built>
  <how-to-verify>
    Setup:
    1. `npm run harness`
    2. `npm start`
    3. Open `http://localhost:4200/dev/worklet-harness.html` with devtools open on the Console tab.
    4. Use headphones at a low starting volume, then raise to a comfortable listening level.

    Checks — report a pass or a specific failure for each:

    **Check 1 — the worklet loads.** Click the enable button. The status line should report the
    context running and the module loaded. The console must show no errors — in particular nothing
    about a failed module fetch, a parse error, or `window`/`document` being undefined, which would
    mean the bundle was not self-contained.

    **Check 2 — no sound before a gesture.** Confirm nothing was audible before you clicked enable,
    and that the page produced no audio on load.

    **Check 3 — the single operator.** Play the single-operator case. You should hear one steady, pure
    sine tone at concert A — smooth, with no buzz, no warble, no crackle, and no drift in pitch over
    ten seconds or so of sustain. A rough or gritty tone would indicate the phase accumulator or the
    render loop is wrong in a way the analytical test somehow missed.

    **Check 4 — the additive case.** Play the additive six-carrier case. You should hear a single
    fused, organ-like tone built from six harmonics over a lower fundamental — richer than Check 3 but
    still one coherent pitch, not six separate notes, and not distorted or buzzy. Distortion here
    would mean the six-carrier sum is clipping despite the safety clamp.

    **Check 5 — clean starts and stops.** Start and stop each case several times, including stopping
    and immediately restarting. There must be no click, pop or thump at either edge.

    **Check 6 — loudness safety.** With your volume set for comfortable listening on the single
    operator, switch to the additive case without touching the volume. It may be somewhat louder, but
    it must not be startling or uncomfortable. This is the six-carrier worst case against the same
    clamp Phase 5 verified.

    **Check 7 — no dropouts.** Let the additive case run for at least thirty seconds. There must be no
    stutters, gaps or glitches, which would indicate allocation or GC pressure on the render thread.

    **Check 8 — the rebuild loop (RESEARCH Assumption A5, unverified).** Change one operator's
    frequency multiplier in `src/app/domain/dx7/dsp/additive-fixture.ts`, run `npm run harness`, and
    reload the page. Report whether the sound changed without restarting the dev server, or whether a
    restart was required. Either answer is fine — it needs recording in the SUMMARY, and the README
    guidance adjusted if a restart turns out to be necessary. Revert the edit afterwards.

    **Check 9 — keyboard operation.** Operate every control using only Tab and Enter or Space, and
    confirm each focused control shows a visible focus indicator.
  </how-to-verify>
  <acceptance_criteria>
    - A human has run the harness in a real browser and reported an explicit pass or a specific failure for each of the nine checks.
    - Check 1 recorded a clean console with the module loaded — this is the ROADMAP's "worklet loads" criterion.
    - Checks 3 and 4 both recorded correct-sounding output — this is the ROADMAP's "runs a single operator and an additive multi-operator case correctly" criterion.
    - Check 8's answer is recorded in `07-03-SUMMARY.md`, and the README's rebuild guidance is corrected if a dev-server restart proved necessary.
    - Any failure is written up with the check number, what was heard or seen, and the browser used, and is fixed before this task is marked done — an approval with unresolved findings is not a pass.
    - On a clean approval with zero findings this task changes no source file; the approval itself and the Check 8 answer are the deliverable.
    - `07-VALIDATION.md` is updated: the manual-only row is marked verified, and `status` and `wave_0_complete` are advanced to reflect the executed phase, following Phase 6's 06-04 precedent.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all nine checks pass, or describe exactly what you heard or saw for any check that failed.</resume-signal>
  <done>A human has confirmed in a real browser that the worklet loads without errors and that both the single-operator and additive multi-operator cases sound correct, click-free and safe, and the rebuild-loop answer is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev harness page → `AudioContext` / speakers | The only place in this phase where code this project ships actually drives a human's audio hardware. |
| Build flag → `public/dev/` | The boundary that decides whether a development-only surface reaches a production artifact. |
| Harness page → `AudioWorkletGlobalScope` | The harness posts messages into the same render thread the engine does, over the same shared contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Information Disclosure | `public/dev/` harness output | low | mitigate | A dev page reaching production is an unadvertised surface that constructs an `AudioContext` on a real user's device. Mitigated for the clean-checkout path: the harness is emitted only by an explicit opt-in flag, its output directory is git-ignored, and none of `prebuild`/`prestart`/`pretest` pass that flag — so a clean checkout's production build cannot contain it. Proven by the build-then-assert-absent gate in Task 1. **Superseded for realistic harness-then-build isolation by 07-04** (output relocated outside `public/`; postbuild + `verify:harness-isolation`). |
| T-07-10 | Denial of Service | Harness `port.postMessage` payloads | low | mitigate | The harness builds every payload through 07-01's shared message builders rather than hand-writing object literals, so it cannot send a shape the adapter's `parseWorkletMessage` guard was not written for; and even if it did, that guard (`T-07-01`) rejects it without throwing. |
| T-07-11 | Denial of Service | Human hearing safety at the harness output | medium | mitigate | Not a STRIDE-classical asset, registered deliberately because it is the one place this phase can actually harm someone. The harness schedules the same `MASTER_GAIN` clamp Phase 5 verified, applies it before the destination, exposes no control that can bypass it, and the checkpoint script instructs starting at low volume on headphones and explicitly tests the six-carrier worst case (Check 6). |
| T-07-12 | Tampering | Stale `public/` artifact during verification | low | mitigate | A human could approve the phase while listening to an old bundle, certifying code that is not what shipped. Mitigated by the lifecycle hooks regenerating the bundle on every build and test run, by the README's explicit warning, and by checkpoint Check 8 which exercises the rebuild loop directly and records the result. |
| T-07-SC | Tampering | package-manager installs | low | accept | This plan installs zero packages; 07-01's blocking legitimacy checkpoint covered the phase's only two additions. Any install task appearing during execution invalidates this row and must stop for the legitimacy protocol. |

</threat_model>

<verification>
- `npm run typecheck:worklet`
- `npm run harness`
- `npm run build:worklet` followed by asserting the harness output directory is absent
- `npm test`
- `npm run build`
- `npm run lint`
- `git diff --exit-code angular.json src/`
- Blocking human listening checkpoint approved with zero unresolved findings

</verification>

<success_criteria>
- The harness builds only on demand, loads the real worklet bundle in a browser, and drives both
  proof cases through the shared message contract with the proven safety clamp applied.
- A production build contains the worklet bundle and no dev directory.
- A human has heard a clean single sine and a clean, fused, comfortably-loud additive six-carrier
  tone, with no console errors, no clicks, no dropouts, and full keyboard operation.
- The rebuild-loop question left open by research is answered and recorded.
- `npm run build`, `npm test`, `npm run lint` and `npm run typecheck:worklet` are all green.
</success_criteria>

<output>
Create `.planning/phases/07-audioworklet-dsp-foundation/07-03-SUMMARY.md` when done.
</output>
