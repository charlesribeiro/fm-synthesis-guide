---
phase: 14-playwright-release
plan: 01
type: plan
wave: 1
autonomous: true
requirements: [RELEASE-01]

estimate:
  tasks: 5
  confidence: high
---
# Phase 14 - Plan 01: Playwright Smoke Suite, CI, Deployment & Docs

<tasks>
<task id="T1">
  <description>Install and configure Playwright</description>
  <action>Initialize Playwright for E2E testing, configured to run against localhost on a specified port. Add scripts to package.json.</action>
</task>
<task id="T2">
  <description>Write basic E2E Smoke Tests</description>
  <action>Write E2E tests covering routing (Learn, Algorithms, Playground), enabling audio, note lifecycle, and algorithm switching.</action>
</task>
<task id="T3">
  <description>Set up GitHub Actions CI</description>
  <action>Extend the existing .github/workflows/ci.yml without removing security settings, harness isolation or typecheck gates to run build, lint, vitest, and playwright tests on push and PR.</action>
</task>
<task id="T4">
  <description>Update documentation for release</description>
  <action>Update README.md with deployment (GitHub Pages/static hosting instructions), methodology, and final release notes.</action>
</task>
<task id="T5" type="checkpoint:decision">
  <description>Confirm hosting, deploy, and verify the real static release</description>
  <action>Confirm GitHub Pages or the user's chosen host. Implement and test base-path and deep-link handling if needed. Publish only production output plus architecture/methodology docs over HTTPS. Verify deployed routes, JS MIME types and real worklet audio before closing RELEASE-01. Do not merge the PR to achieve deployment.</action>
</task>
</tasks>

<success_criteria>
- Playwright E2E tests pass reliably locally.
- CI pipeline is defined and executes Playwright tests.
- Documentation reflects the final state and deployment steps.
- A real static deployment and published architecture/methodology docs are verified (roadmap criterion 3).
</success_criteria>

## Resume record — 2026-09-21

T1–T4 implemented and locally verified. User selected GitHub Pages; T5 deployment
implementation is complete, with live publication/verification deferred until merge. The inherited plan omitted actual deployment from its task list;
T5 restores the authoritative roadmap criterion rather than expanding RELEASE-01.
Do not mark phase complete on tests alone. Parent agent executing inline because the
configured delegation model rejected its context window before spawning any worker.

## Approved deployment decision

Canonical URL: https://charlesribeiro.github.io/fm-synthesis-guide/.
Use production base href, base-aware fixed worklet URL, and a custom 404 SPA shell.
CI uploads the tested artifact only on main push; the dependent reusable Pages workflow
deploys it. User explicitly permits opening the PR before live publication, provided
RELEASE-01 stays pending and exact post-merge verification is documented. Do not merge.
