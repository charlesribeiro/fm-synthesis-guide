---
phase: 11-curriculum-across-all-32-algorithms
plan: 04
subsystem: domain/dx7/lessons
tags: [dx7, fm-synthesis, lesson-content, tdd, invariants, curriculum-completeness]

requires:
  - phase: 11-01
    provides: structuralLesson factory, buildStructuralStartingPatch/buildStructuralTryThis, selectTryThisTarget, isAdditiveLikeAlgorithm, hop-distance and derive-role helpers, curriculum-order LESSON_IDS convention
  - phase: 11-02
    provides: CURRICULUM_GROUP_ORDER, groupLessonsByTeachingTag (lesson-grouping.ts)
  - phase: 11-03
    provides: eleven Additive Stacks + Tree/Branch lessons (Algorithms 2-12), prose-shape invariant suite, cross-check table conventions
provides:
  - thirteen new LESSONS rows (Algorithms 13 through 25), each built through the shared structuralLesson factory with no hand-picked operator value
  - LESSON_IDS/LessonId grown from nineteen to thirty-two members — the complete curriculum, one lesson per canonical algorithm
  - a proven lesson-to-algorithm bijection (CURR-01's "every algorithm" clause, checked against the live datasets rather than a literal count)
  - a curriculum-ordering invariant checked against the dataset's own teaching tags via CURRICULUM_GROUP_ORDER, not against a copied literal array
  - groupLessonsByTeachingTag proven to yield four non-empty groups of 7/6/12/7 lessons over the complete dataset
  - a whole-dataset isAdditiveLikeAlgorithm membership proof (21, 23, 24, 25, 29, 31, 32) plus its ratio consequence on shipped lesson data
  - all five duplicate-topology clusters (13/14; 3/4/11; 24/25/31; 2/12; 5/6) proven pairwise-equal, and Algorithm 1 proven NOT equal to Algorithm 13 despite identical routing
  - the try-this expected-target cross-check table extended to Algorithms 13-25, including the Algorithm 16/17 feedback-relocation proof and the Algorithm 24/25/31 identical-target proof
  - Algorithm 19's unresolved-provenance decision recorded and tested (ordinary lesson row, no caveat/badge)
affects: [11-05-grouped-index]

requirements-completed: []

actuals:
  tokens: 12776
  tasks: 3
  commits: 4

tech-stack:
  added: []
  patterns:
    - "Per-task LessonId/LESSON_IDS growth kept strictly inside each task's own <action> boundary (6 members for Task 1, 7 for Task 2) rather than growing the union to its final size early — an early attempt to grow both at once left the test suite red until Task 2's rows existed, so the type growth was rolled back to match the task boundary exactly, keeping every commit green (see Deviations)."
    - "Cross-check tables continue to be hand-transcribed from the plan's own edge-list tables, never derived by calling the function under test, extended one more time to Algorithms 13-25."
    - "Whole-dataset invariants (bijection, curriculum ordering via CURRICULUM_GROUP_ORDER, additive-like membership, duplicate-cluster equality) iterate the live ALGORITHMS/LESSONS arrays rather than asserting literal counts, so a future dataset change fails the right test instead of silently going stale."

key-files:
  created: []
  modified:
    - src/app/domain/dx7/lessons/lesson-definition.ts
    - src/app/domain/dx7/lessons/lessons.ts
    - src/app/domain/dx7/lessons/lessons.spec.ts
    - src/app/domain/dx7/lessons/try-this-selection.spec.ts
    - src/app/domain/dx7/lessons/structural-lesson-patch.spec.ts
    - src/app/features/learn/lesson-detail/lesson-detail.spec.ts

key-decisions:
  - "Task-scoped LessonId/LESSON_IDS growth: initially grew both to all thirteen new members in one edit while only adding Task 1's six lesson rows, which reproduced the exact predicted npm test failures (length/order mismatches, getLesson RangeError for the not-yet-added algorithm-19). Reverted the type/array growth back to Task 1's literal six-member scope before committing, then re-grew it to the full thirty-two only when Task 2's seven rows landed alongside it — keeping every commit's test/lint/build gate green, per CLAUDE.md's 'run the gates before declaring work complete' rule."
  - "Spec files outside each task's literal <files> list (lesson-detail.spec.ts, structural-lesson-patch.spec.ts, try-this-selection.spec.ts, lessons.spec.ts) were touched anyway because each task's own acceptance criteria explicitly required assertions that could only live in those files — mirrors the file-scoping precedent already documented in 11-03-SUMMARY.md."
  - "Algorithm 19's reviewStatus: 'unresolved' flag is handled exactly as 11-RESEARCH.md's flagged assumption recommended: the lesson is generated by the same rules as every other row, with no caveat, badge, or special-cased field added to its data or prose. Tested directly (isUnresolvedAlgorithm(algorithm19) === true, and the lesson row has the same field shape as every other row) so the decision is checked, not just narrated. Raised here for the 11-05 checkpoint per the plan's own instruction."

patterns-established:
  - "Whole-curriculum invariants (bijection, ordering-against-tags, grouped-shape, additive-like-membership, duplicate-cluster-equality) as a distinct final-task category, separate from per-row prose/preset checks — proves the aggregate claims (CURR-01, D-01/D-02/D-03, D-13) as checked facts rather than as an emergent property of thirty-two individually-correct rows."

metrics:
  duration: "~35min"
  completed: 2026-08-24
status: complete
---

# Phase 11 Plan 04: Algorithms 13-25 lessons (Tree and Branch completion + all of Rooting) + whole-curriculum invariants Summary

**Thirteen new structurally-generated lessons (Algorithms 13-25) complete every one of the thirty-two canonical algorithms' curriculum coverage, then a dedicated invariant suite turns "every algorithm has a lesson, correctly ordered and grouped" from a narrated claim into a checked fact against the live datasets.**

## Performance

- **Duration:** ~35 min
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Algorithms 13 through 18 (completing the Tree and Branch group at twelve lessons) and Algorithms 19 through 25 (the entire Rooting group) each have a lesson, an original derived starting patch, and a derived try-this experiment — `LESSON_IDS`/`LESSONS` grew from 19 to 32 members, reaching the full curriculum.
- The Algorithm 1/13/14 three-way identical cluster and the Algorithm 24/25/31 three-way identical cluster both say so plainly in their prose rather than inventing a routing difference; Algorithm 22 is taught as the instructive near-miss against Algorithm 21 (same carrier count, but none of Algorithm 22's carriers is bare); Algorithm 15's role derivation is taken from `deriveCarriers`/`deriveIsolatedCarriers`, not from the dataset's loose free-text "carrier 2" name.
- Algorithm 19's `unresolved` provenance flag is handled exactly per the plan's flagged assumption: an ordinary lesson row generated by the same rules as every other, no caveat or badge added, and the decision is tested directly rather than left implicit.
- A dedicated whole-curriculum invariant suite (Task 3) proves: the lesson-to-algorithm bijection (CURR-01's "every algorithm" clause); the curriculum ordering checked against `CURRICULUM_GROUP_ORDER` and the dataset's own teaching tags rather than a copied literal array; `groupLessonsByTeachingTag` yielding four non-empty groups of 7/6/12/7 lessons; the `isAdditiveLikeAlgorithm` whole-dataset membership (21, 23, 24, 25, 29, 31, 32) and its ratio consequence; all five duplicate-topology clusters proven pairwise-equal (with Algorithm 1 proven NOT equal to Algorithm 13 despite identical routing); the try-this cross-check table extended through Algorithm 25, including the Algorithm 16/17 feedback-relocation proof and the Algorithm 24/25/31 identical-target proof.

## Task Commits

Each task was committed atomically, with one additional test-only commit inside Task 1 to close a gap between the task's literal `<files>` scope and its own acceptance criteria (see Deviations):

1. **Task 1: The Tree and Branch group's last six lessons** - `e1c8ee0` (feat)
2. **Task 1 acceptance-criteria coverage** - `3fcdb24` (test — see Deviations)
3. **Task 2: The Rooting group's seven lessons** - `e02b9a2` (feat)
4. **Task 3: Whole-curriculum completeness, ordering and consistency invariants** - `c5b6dc2` (test)

## Files Created/Modified

- `src/app/domain/dx7/lessons/lesson-definition.ts` - `LessonId` union and `LESSON_IDS` grown to thirty-two members (algorithm-13 through algorithm-25 appended in curriculum order)
- `src/app/domain/dx7/lessons/lessons.ts` - thirteen new `LESSONS` rows, each via `structuralLesson`
- `src/app/domain/dx7/lessons/lessons.spec.ts` - whole-curriculum bijection, ordering, grouped-shape, feedback-consistency, and final-guard-slug invariants (Task 3)
- `src/app/domain/dx7/lessons/try-this-selection.spec.ts` - expected-target table for Algorithms 13-25, Algorithm 16/17 feedback-relocation proof, Algorithm 24/25/31 identical-target proof
- `src/app/domain/dx7/lessons/structural-lesson-patch.spec.ts` - Task 1/2 per-algorithm acceptance-criteria coverage, whole-dataset additive-like membership, five-cluster duplicate-equality table, Algorithm 19 unresolved-provenance proof
- `src/app/features/learn/lesson-detail/lesson-detail.spec.ts` - cold-deep-link coverage for Algorithms 15, 17, 19, and 24

## Decisions Made

See `key-decisions` in frontmatter: task-scoped `LessonId`/`LESSON_IDS` growth (reverted an over-eager single-edit growth to keep every commit green), spec files touched outside each task's literal `<files>` list because the task's own acceptance criteria required it, and Algorithm 19's unresolved-provenance handling tested directly.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — blocking issue, self-caused] Over-eager `LessonId`/`LESSON_IDS` growth reverted to Task 1's literal scope**
- **Found during:** Task 1, immediately after growing `lesson-definition.ts` to all thirteen new members (13-25) in one edit while only adding Task 1's six lesson rows to `lessons.ts`.
- **Issue:** `npm test` failed with three predictable errors: `LESSONS` had 25 rows but `LESSON_IDS` claimed 32, and `getLesson('algorithm-19')` threw a `RangeError` since Task 2 hadn't added that row yet.
- **Fix:** Reverted `lesson-definition.ts`'s `LessonId` union and `LESSON_IDS` array to exactly Task 1's six new members (13 through 18), re-ran the full gate (green), committed Task 1, then grew the type/array again to the full thirty-two only when Task 2's seven rows landed alongside it in the same commit.
- **Files modified:** `src/app/domain/dx7/lessons/lesson-definition.ts` (edited twice: once for Task 1's six members, once for Task 2's remaining seven).
- **Verification:** `npm test`/`npm run lint`/`npm run build` all green at both the Task 1 and Task 2 commits.
- **Committed in:** `e1c8ee0` (Task 1), `e02b9a2` (Task 2)

---

**2. [Rule 2 — missing test coverage the plan's own acceptance criteria requires] Spec files touched outside each task's literal `<files>` list**
- **Found during:** Task 1 and Task 2, while implementing each task's acceptance criteria.
- **Issue:** Task 1's `<files>` list is `lesson-definition.ts, lessons.ts` only, but its acceptance criteria explicitly require specs asserting the 13/14 preset equality, the 16/17/18 single-carrier shape, Algorithm 15's role derivation, and cold-deep-link coverage for `/learn/algorithm-15` and `/learn/algorithm-17` — none of which live in the two listed files. Task 2's acceptance criteria have the same shape for Algorithms 19-25.
- **Fix:** Added the required assertions to `structural-lesson-patch.spec.ts` and `lesson-detail.spec.ts`, following the exact same file-scoping precedent 11-03-SUMMARY.md already documents for this project.
- **Files modified:** `src/app/domain/dx7/lessons/structural-lesson-patch.spec.ts`, `src/app/features/learn/lesson-detail/lesson-detail.spec.ts`.
- **Verification:** `npm test`/`npm run lint`/`npm run build` all green.
- **Committed in:** `3fcdb24` (Task 1's coverage), `e02b9a2` (Task 2, combined with its production rows)

---

**Total deviations:** 2 (both process/scope-boundary corrections; no behavior, prose content, or acceptance-criteria change). Every task's `<behavior>`/`<action>`/`<acceptance_criteria>` item is implemented and verified exactly as specified.
**Impact on plan:** None on delivered functionality.

## Issues Encountered

None beyond the self-caught task-boundary issue documented above — no external blockers, no auth gates, no package installs.

## TDD Gate Compliance

Task 3 is `tdd="true"`. Every assertion in Task 3 was written against Tasks 1 and 2's already-shipped data (the thirteen new lesson rows and the `structuralLesson`/`selectTryThisTarget`/`isAdditiveLikeAlgorithm` machinery, all already correct from earlier plans in this phase) and passed on first run — no RED phase was observed. This is the same "fix attempt finds nothing to fix" substitution this project's `STATE.md` already documents for plans 02-03, 03-01, 04-01, 08-01, 11-01 and 11-03: each assertion was hand-verified against the live `ALGORITHMS`/`LESSONS` data before being written (the expected-target tables, the additive-like membership set, and the five duplicate clusters were all cross-checked against `11-RESEARCH.md`'s structural survey and Tasks 1-2's own action tables before the test files were edited), which serves the same "prove the test isn't vacuously true" purpose a formal RED phase would, per this plan's own TDD instruction.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- CURR-01's **curriculum-data** portion is satisfied: all thirty-two algorithms have a lesson, an original derived preset, and a derived experiment, proven by the Task 3 bijection test rather than asserted by row count. Full CURR-01 completion (grouped `/learn` index and progress tracking, D-13/D-14/D-16) is deferred to plan 11-05. `requirements mark-complete` is left to the orchestrator per this plan's execution contract (worktree mode; STATE.md/ROADMAP.md/REQUIREMENTS.md are updated centrally after merge).
- Plan 11-05 (grouped `/learn` index) now has a complete, ordered, four-group dataset to render: `LESSON_IDS` is the final thirty-two-member curriculum order, `groupLessonsByTeachingTag(LESSONS, ALGORITHMS)` returns four non-empty groups sized 7/6/12/7 in curriculum order, and every group's lessons are a contiguous slice of `LESSONS` — all proven by this plan's Task 3, not left for 11-05 to discover.
- **Raised explicitly for the 11-05 checkpoint, per this plan's own flagged assumption:** Algorithm 19 carries `reviewStatus: 'unresolved'` in the canonical dataset. Its lesson was generated by exactly the same rules as every other row, with no caveat, badge, or special UI added — this plan's position (and 11-RESEARCH.md's) is that the flag is provenance metadata about the dataset's edge-list confidence, not a statement about the routing's runtime validity, and surfacing it in learner-facing UI would be new scope this phase never agreed. The developer should confirm or overturn this at the 11-05 checkpoint.
- **Blocker/concern for a later plan, not this one:** the thirteen new presets' *musical* usefulness (do they sound like good teaching starting points, not just valid patches) is explicitly out of scope for automated verification in this plan, per this project's established `must_haves` "backstop" convention — plan 11-05's blocking listening and reading checkpoint is the intended backstop, and it now has all thirty-two rows to check rather than a partial set.
- No other blockers.

## Self-Check: PASSED

All 6 modified files confirmed present on disk with the expected content; commits `e1c8ee0`, `3fcdb24`, `e02b9a2`, and `c5b6dc2` confirmed present in `git log`.

---
*Phase: 11-curriculum-across-all-32-algorithms*
*Completed: 2026-08-24*
