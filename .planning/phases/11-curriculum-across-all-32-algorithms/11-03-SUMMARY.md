---
phase: 11-curriculum-across-all-32-algorithms
plan: 03
subsystem: domain/dx7/lessons
tags: [dx7, fm-synthesis, lesson-content, tdd, invariants]

requires:
  - phase: 11-01
    provides: structuralLesson factory, buildStructuralStartingPatch/buildStructuralTryThis, hop-distance and derive-role helpers, eight-member LESSON_IDS curriculum sequence
provides:
  - eleven new LESSONS rows (Algorithms 2 through 12), each built through the shared structuralLesson factory with no hand-picked operator value
  - LESSON_IDS/LessonId grown from eight to nineteen members, curriculum order preserved
  - hand-populated try-this expected-target table (Algorithms 2-12) and feedback-relocation cross-checks (2 v 1, 8 v 9, 10 v 3) in try-this-selection.spec.ts
  - duplicate-cluster converse proofs (3/4/11 and 2/12: equal edges, equal feedback operator, equal try-this target)
  - generic prose-shape invariant suite over every LESSONS row (paragraph count, D-12 objective grammar, trimmed non-emptiness, algorithm-<id> naming, algorithmId uniqueness) — written against LESSON_IDS/LESSONS length, not a literal count
affects: [11-04-thirteen-more-lessons, 11-05-grouped-index]

requirements-completed: [CURR-01]

actuals:
  tokens: 9143
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Generic describe.each prose-shape suite over LESSONS (not a hardcoded row count) — plan 11-04's thirteen further rows inherit it with zero spec edits, same convention as the existing dataset-invariant suite"
    - "Independently-authored second-witness tables for try-this targets, mirroring lessons.spec.ts's EXPECTED_ALGORITHM_1_CARRIERS convention: the table is transcribed by hand from the edge lists, not generated by calling the rule under test"

key-files:
  created: []
  modified:
    - src/app/domain/dx7/lessons/lesson-definition.ts
    - src/app/domain/dx7/lessons/lessons.ts
    - src/app/domain/dx7/lessons/lessons.spec.ts
    - src/app/domain/dx7/lessons/try-this-selection.spec.ts
    - src/app/features/learn/lesson-detail/lesson-detail.spec.ts

key-decisions:
  - "Task 2's commit (3909de3) front-loaded most of Task 3's cross-check suite — the hand-populated expected-target table, the three feedback-relocation pair proofs, and Algorithm 7's hop-4 output-level/isolated-carrier facts — alongside the six lesson rows it was already adding, since writing the structural proof beside the structural data it proves was more natural than deferring it a task. Task 3 completed the remainder exactly as planned: the prose-shape invariant suite and the two duplicate-cluster converse cases (3/4/11, 2/12) that Task 2 had not yet touched."
  - "Execution was interrupted after Task 2's commit and resumed in a later session. Task 3's spec changes were already fully written (uncommitted) in the working tree at resume time; per the safe-resume 'close out manually' path, they were verified against npm test / npm run lint / npm run build rather than rewritten, then committed as-is once confirmed green (1739/1739 tests, no lint errors, build succeeds). No production code changed during closeout."

patterns-established:
  - "Cross-check tables are hand-transcribed from the plan's own edge-list table, never derived by calling the function under test — keeps the proof independent of the implementation it's checking."

metrics:
  duration: "~4min active (Tasks 1-2, same session) + resume-and-verify closeout for Task 3"
  completed: 2026-08-24
status: complete
---

# Phase 11 Plan 03: Algorithms 2-12 lessons (Additive Stacks completion + Tree and Branch opening six) Summary

**Eleven new structurally-generated lessons (Algorithms 2-12) plus the cross-check and prose-shape test suites that pin their correctness — completing the Additive Stacks group and opening Tree and Branch, all through the existing `structuralLesson` factory with zero hand-picked operator values.**

## Performance

- **Duration:** ~4 min active work across Tasks 1-2 (23:09-23:13 same evening); Task 3's spec work was already written at session-resume and was verified, not rewritten, before this closeout commit.
- **Started:** 2026-08-23T23:05:00-03:00 (approx, first LESSON_IDS growth)
- **Completed:** 2026-08-24T13:04:56-03:00
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments
- Algorithms 2 through 6 (Additive Stacks group, completing it at six lessons alongside Algorithm 1) and Algorithms 7 through 12 (Tree and Branch group's first six) each have a lesson, an original derived starting patch, and a derived try-this experiment — `LESSON_IDS`/`LESSONS` grew from 8 to 19 members.
- Every duplicate-topology pair the canonical dataset declares (3≡4, 5≡6, 3≡4≡11, 2≡12) says so honestly in its prose rather than inventing a routing difference, and every feedback-relocation pair (2 v 1, 8 v 9, 10 v 3) is proven by test to target a different try-this operator despite sharing non-feedback edges.
- A generic, row-count-independent prose-shape invariant suite now runs over every `LESSONS` row — paragraph count, objective grammar, non-emptiness, id-naming convention, id uniqueness — so plan 11-04's thirteen remaining rows are checked automatically with no spec changes.

## Task Commits

Each task was committed atomically:

1. **Task 1: The Additive Stacks group's five remaining lessons** - `de176a0` (feat)
2. **Task 2: The Tree and Branch group's first six lessons** - `3909de3` (feat — also front-loaded most of Task 3's cross-check suite; see Deviations)
3. **Task 3: Cross-check tables and prose-shape invariants over every shipped row** - `6b0cb1d` (test)

_Note: Task 3 is TDD-tagged; see Deviations for why its commit did not follow a literal RED→GREEN cycle._

## Files Created/Modified
- `src/app/domain/dx7/lessons/lesson-definition.ts` - `LessonId` union and `LESSON_IDS` grown to nineteen members (algorithm-2 through algorithm-12 appended in curriculum order)
- `src/app/domain/dx7/lessons/lessons.ts` - eleven new `LESSONS` rows, each via `structuralLesson`
- `src/app/domain/dx7/lessons/lessons.spec.ts` - generic prose-shape invariant suite (Task 3)
- `src/app/domain/dx7/lessons/try-this-selection.spec.ts` - expected-target table for Algorithms 2-12, feedback-relocation proofs, duplicate-cluster converse proofs
- `src/app/features/learn/lesson-detail/lesson-detail.spec.ts` - cold-deep-link route coverage for Algorithms 2, 4, 7, 9

## Decisions Made
See `key-decisions` in frontmatter: Task 2 absorbed most of Task 3's cross-check-suite scope because it was more natural to write beside the lesson data it proves; Task 3's closeout on resume verified already-written work rather than re-deriving it.

## Deviations from Plan

### Auto-fixed Issues

**1. [Task boundary shift] Cross-check suite written one task earlier than planned**
- **Found during:** Task 2 (Tree and Branch group's first six lessons)
- **Issue:** The plan assigns `try-this-selection.spec.ts` to Task 3 only. While authoring Task 2's six lessons, the hand-populated expected-target table and the feedback-relocation/hop-4 proofs were written alongside the lesson rows they check, ahead of Task 3.
- **Fix:** No fix needed — Task 3's acceptance criteria for that content were already satisfied when Task 3 began. Task 3 completed only the remaining, genuinely Task-3-scoped pieces: the prose-shape suite and the two duplicate-cluster converse cases.
- **Files modified:** `src/app/domain/dx7/lessons/try-this-selection.spec.ts` (touched a task earlier than the plan's file-per-task mapping specified)
- **Verification:** `npm test`, `npm run lint`, `npm run build` all green at both Task 2's commit and Task 3's commit.
- **Committed in:** `3909de3` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (task-boundary shift, no behavior or scope change)
**Impact on plan:** None on scope or correctness — every acceptance criterion in both tasks is satisfied by the final state. Purely a difference in which commit certain lines landed in.

## Issues Encountered
Execution was interrupted mid-plan after Task 2's commit; the session resumed later with Task 3's spec edits already present but uncommitted in the working tree. Recovery followed the "close out manually" path: the existing changes were verified (not rewritten) against `npm test` (1739/1739 passing, 52 files), `npm run lint` (clean), and `npm run build` (succeeds, `assert-no-harness-in-dist` passes) before committing. No genuine RED phase was observed for Task 3's new assertions since Tasks 1-2's data already satisfied them — the same "fix attempt finds nothing to fix" situation this repository's STATE.md already records for plans 02-03, 03-01, 04-01, 08-01, and 11-02 Task 2.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 11-04 (Algorithms 13-25, completing all thirty-two) can proceed: it depends only on 11-03, and the prose-shape invariant suite it inherits requires no edits to extend.
- Plan 11-05 (grouped `/learn` index) still additionally depends on 11-02's grouping transform; both prerequisites are now complete.
- No blockers.

---
*Phase: 11-curriculum-across-all-32-algorithms*
*Completed: 2026-08-24*
