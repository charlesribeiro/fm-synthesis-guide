---
phase: 10-visualizers-and-comparison-tools
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/app/features/playground/visualizer/spectrum-scale.ts
  - src/app/features/playground/visualizer/spectrum-scale.spec.ts
  - src/app/features/playground/visualizer/visualizer-frame.ts
  - src/app/features/playground/visualizer/visualizer-frame.spec.ts
  - src/app/features/playground/visualizer/visualizer.ts
  - src/app/features/playground/visualizer/visualizer.html
  - src/app/features/playground/visualizer/visualizer.scss
  - src/app/features/playground/visualizer/visualizer.spec.ts
  - src/app/features/playground/playground.ts
  - src/app/features/playground/playground.spec.ts
autonomous: true
requirements: [VIZ-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The spectrum lane renders as discrete vertical bars, not a continuous line, so it reads unambiguously as per-band amplitude and stays visually distinct from the oscilloscope's trace (D-07)."
    - "Bar band edges are geometrically spaced between a 20 Hz floor and a 20 kHz ceiling, so each successive bar covers the same musical interval rather than the same number of hertz, and FM sidebands near the fundamental stay legible instead of being crushed into the leftmost pixels (D-05)."
    - "Bin 0 of the frequency data — the DC component at 0 Hz, where a logarithmic position is undefined — is never mapped onto the axis; the display starts at the 20 Hz floor and no bar is ever drawn at a non-finite x-coordinate (Pitfall 3)."
    - "Each bar's height comes from the maximum byte across the linear bins that fall inside its band, so a wide high-frequency band covering many bins does not read as artificially quiet next to a narrow low-frequency band covering one."
    - "The frequency axis carries drawn tick labels reading 100 Hz, 1 kHz and 10 kHz, positioned by the same logarithmic mapping the bars use, so a label and the bar beneath it can never disagree (D-06)."
    - "The spectrum also carries an accessible text description in the DOM next to the canvas, because canvas pixels are invisible to assistive technology — the same rule already applied to the SVG algorithm diagrams (D-06)."
    - "Bar band-to-bin ranges are computed from the engine's reported analysis sample rate rather than a hardcoded rate, are computed once when a non-zero rate first becomes available, and are reused unchanged on every subsequent frame."
    - "When the user has asked for reduced motion, the display still shows live data but repaints at a low fixed rate instead of every frame — it is neither frozen nor animated at full speed (CLAUDE.md: respect reduced motion)."
    - "The frequency buffer is a separate, `ANALYSER_FREQUENCY_BIN_COUNT`-length object from the oscilloscope's `ANALYSER_FFT_SIZE`-length buffer; the two are never shared or swapped (Pitfall 5)."
    - "Playground's coming-soon list no longer claims the oscilloscope and spectrum display is a later-phase capability, and still lists the two capabilities that genuinely remain future work."
    - statement: "The default decibel range of the platform's analyser renders bars at a readable height against this project's master gain of one sixth, without needing the decibel-range members to be added to the boundary surface and tuned — if the bars read as saturated or near-invisible in a real browser, the tuning is a constant change surfaced at plan 10-04's checkpoint, not a structural one."
      verification: backstop
  artifacts:
    - src/app/features/playground/visualizer/spectrum-scale.ts
    - src/app/features/playground/visualizer/spectrum-scale.spec.ts
    - "src/app/features/playground/visualizer/visualizer-frame.ts (extended: `drawSpectrum`, `drawFrequencyAxis`, spectrum geometry and colour constants)"
    - "src/app/features/playground/visualizer/visualizer.ts (extended: spectrum canvas, frequency buffer, bar-range caching, reduced-motion throttle, spectrum description)"
    - "src/app/features/playground/visualizer/visualizer.html (extended: second canvas and its description)"
    - "src/app/features/playground/playground.ts (reduced: the oscilloscope-and-spectrum coming-soon entry removed)"
  key_links:
    - "`engine.getAnalysisSampleRate()` -> `buildBarBinRanges(sampleRate, ANALYSER_FFT_SIZE, ANALYSER_FREQUENCY_BIN_COUNT, SPECTRUM_BAR_COUNT)` -> cached ranges -> `drawSpectrum(ctx, freqBuffer, ranges, geometry)` -> canvas bars"
    - "`hzToFraction` is the single logarithmic position function: `barEdgesHz` lays the bands out over the same range and `drawFrequencyAxis` places the tick labels through it, so a label and the bar beneath it are placed by one mapping and cannot disagree"
    - "`MotionPreference.prefersReducedMotion` is read imperatively inside the frame callback — a signal read in a non-reactive context, which creates no dependency and schedules no change detection — and gates the repaint against the timestamp the frame scheduler passes in"
  prohibitions:
    - statement: "MUST NOT present the spectrum as an exact or authoritative analysis of a real DX7. The project's standing rule is that this engine is an educational approximation and must never be documented or labelled as a bit-accurate emulation; a display whose surrounding copy implies the bars show what a hardware DX7 would produce converts an honest approximation into a false claim, and does so in the one place a learner is most likely to trust it."
      status: resolved
      verification: judgment
    - statement: "MUST NOT ship a display whose meaning exists only in canvas pixels. A canvas is opaque to assistive technology, so a spectrum with axis ticks drawn but no equivalent text is not a labelled display for a screen-reader user — it is an unlabelled one. The text description is part of the deliverable, not decoration on top of it."
      status: resolved
      verification: test
    - statement: "MUST NOT reproduce, trace, or closely imitate Yamaha DX7 panel artwork, Dexed's interface, or any other existing product's spectrum-display chrome. The project's licensing rule forbids copied diagrams and panel artwork outright; a graphic-EQ styling that reads as a specific product's is the same violation wearing a different name."
      status: resolved
      verification: judgment
---

<objective>
Complete the labelled spectrum half of VIZ-01: a pure logarithmic band-mapping
module, a bar renderer with drawn frequency tick labels, a second canvas wired
into the existing visualizer against the same analyser tap, an accessible text
description for both lanes, and a reduced-motion repaint rate.

Purpose: plan 10-01 proved the tap, the loop and the draw seam on one waveform.
Everything here builds outward from that proven slice — the frequency lane reuses
the same tap, the same injected scheduler, the same drawing surface and the same
allocate-once discipline, so the only genuinely new work is the logarithmic axis
math, which is isolated into a pure module that can be tested to the exact bin
index without a component fixture in sight.

Output: a Playground visualizer panel where a held note shows both its waveform
and its harmonic content, with a readable frequency axis and a description a
screen reader can use.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visualizers-and-comparison-tools/10-CONTEXT.md
@.planning/phases/10-visualizers-and-comparison-tools/10-RESEARCH.md
@.planning/phases/10-visualizers-and-comparison-tools/10-PATTERNS.md
@.planning/phases/10-visualizers-and-comparison-tools/10-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces (introduced by this plan)

New files:
- `src/app/features/playground/visualizer/spectrum-scale.ts`
- `src/app/features/playground/visualizer/spectrum-scale.spec.ts`

New exported symbols:
- `MIN_DISPLAY_HZ` — `20` (`features/playground/visualizer/spectrum-scale.ts`)
- `MAX_DISPLAY_HZ` — `20000` (`features/playground/visualizer/spectrum-scale.ts`)
- `SPECTRUM_BAR_COUNT` — `32` (`features/playground/visualizer/spectrum-scale.ts`)
- `AXIS_TICK_HZ` — the frozen tick list `[100, 1000, 10000]` (`features/playground/visualizer/spectrum-scale.ts`)
- `formatTickLabel(hz: number): string` — `100` becomes `100 Hz`, `1000` becomes `1 kHz` (`features/playground/visualizer/spectrum-scale.ts`)
- `barEdgesHz(barCount: number): readonly number[]` (`features/playground/visualizer/spectrum-scale.ts`)
- `binIndexForHz(hz, sampleRate, fftSize, binCount): number` (`features/playground/visualizer/spectrum-scale.ts`)
- `hzToFraction(hz: number): number` — logarithmic position in `0..1` (`features/playground/visualizer/spectrum-scale.ts`)
- `BarBinRange` — `{ readonly startBin: number; readonly endBin: number }` (`features/playground/visualizer/spectrum-scale.ts`)
- `buildBarBinRanges(sampleRate, fftSize, binCount, barCount): readonly BarBinRange[] | null` (`features/playground/visualizer/spectrum-scale.ts`)
- `SPECTRUM_CSS_WIDTH`, `SPECTRUM_CSS_HEIGHT`, `SPECTRUM_AXIS_BAND_HEIGHT` (`features/playground/visualizer/visualizer-frame.ts`)
- `BAR_FILL_COLOR`, `AXIS_LABEL_COLOR`, `AXIS_LABEL_FONT` (`features/playground/visualizer/visualizer-frame.ts`)
- `drawSpectrum(ctx, data, ranges, geometry)` (`features/playground/visualizer/visualizer-frame.ts`)
- `drawFrequencyAxis(ctx, geometry)` (`features/playground/visualizer/visualizer-frame.ts`)
- `drawEmptySpectrum(ctx, geometry)` (`features/playground/visualizer/visualizer-frame.ts`)
- `REDUCED_MOTION_FRAME_INTERVAL_MS` — `100` (`features/playground/visualizer/visualizer.ts`)

<flagged_assumptions>
- The spec-less edge probe left VIZ-01 `unresolved` — see plan 10-01's flagged assumptions for
  the full note. The spectrum truths above are derived by hand from the ROADMAP success
  criterion and from D-05, D-06, D-07 and D-08; none of them came from the probe.
- `10-RESEARCH.md` Assumption A3 is carried unresolved into this plan: the analyser's default
  decibel window is assumed to render readable bar heights against this project's master gain
  without adding the decibel-range members to the boundary surface. It is recorded as a
  backstop truth above and is a checkpoint item in plan 10-04. If tuning turns out to be
  needed, it is two more members on `AnalyserNodeLike` and two assignments at construction —
  a constant-shaped change, not a structural one.
- A geometric bar layout over 20 Hz to 20 kHz at 32 bars gives roughly a one-third-octave
  band per bar. At a 44.1 kHz sample rate and an FFT size of 2048 each linear bin is about
  21.5 Hz wide, so the lowest few bars map to the same one or two bins and will move together.
  That is inherent to a fixed FFT size on a logarithmic axis, not a bug; `buildBarBinRanges`
  therefore guarantees every band covers at least one bin rather than pretending otherwise.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: The logarithmic band map — a pure spectrum-scale module and its bounds proofs</name>

  <reversibility rating="reversible">The frequency floor, ceiling, bar count and tick list are four constants in one pure module with no persisted or cross-module contract; changing any of them re-renders differently and touches nothing else.</reversibility>

  <read_first>
    - src/app/features/playground/visualizer/visualizer-frame.ts
    - src/app/features/playground/visualizer/visualizer-frame.spec.ts
    - src/app/core/audio/synth-engine.ts
    - src/app/domain/dx7/audio/value-conversion.ts
    - .planning/phases/10-visualizers-and-comparison-tools/10-RESEARCH.md
  </read_first>

  <behavior>
    - `barEdgesHz(32)` returns exactly 33 entries, strictly increasing, starting at `MIN_DISPLAY_HZ` and ending at `MAX_DISPLAY_HZ`, with every adjacent pair sharing the same ratio to within floating-point tolerance.
    - `hzToFraction(MIN_DISPLAY_HZ)` is `0`, `hzToFraction(MAX_DISPLAY_HZ)` is `1`, and `hzToFraction` is strictly increasing across the range. The geometric midpoint of the range maps to `0.5`.
    - `hzToFraction` clamps rather than returning a non-finite value: `0`, a negative frequency and a frequency above the ceiling all return a value within `0..1`, and none returns `NaN` or an infinity.
    - `binIndexForHz` maps a frequency to a bin index that is always an integer within `0..binCount - 1` inclusive, for frequencies at, below and above both ends of the display range, and for a sample rate of zero.
    - `buildBarBinRanges` returns `null` for a sample rate that is zero, negative or non-finite, so a caller polling before the audio context exists gets an explicit "not yet" rather than a range list full of `NaN`.
    - For a valid 44100 sample rate with an FFT size of 2048 and 1024 bins, `buildBarBinRanges` returns exactly `SPECTRUM_BAR_COUNT` ranges; every range has `startBin <= endBin`; every range's bins lie within `0..1023`; no range starts at bin 0; ranges are non-decreasing across the list; and every range covers at least one bin.
    - `formatTickLabel` returns `100 Hz` for 100, `1 kHz` for 1000, and `10 kHz` for 10000.
  </behavior>

  <files>src/app/features/playground/visualizer/spectrum-scale.ts, src/app/features/playground/visualizer/spectrum-scale.spec.ts</files>

  <action>
Create `spectrum-scale.ts` as a pure module in the visualizer folder. It must import no symbol from Angular and no symbol from the audio layer; it takes every input it needs as a plain numeric parameter. Open the file with a header stating that it owns one thing — the mapping between a frequency in hertz, a horizontal position on the display, and the linear frequency-data bin indices that fall inside a band — and that this is display math, not DX7 domain knowledge, which is why it lives beside the component rather than in the domain layer whose purity gate governs instrument modelling.

Export `MIN_DISPLAY_HZ` at `20` and `MAX_DISPLAY_HZ` at `20000`, documenting that the floor is non-zero because a logarithmic position is undefined at 0 Hz and because bin 0 of the frequency data is the DC component, which carries no musical information (Pitfall 3). Export `SPECTRUM_BAR_COUNT` at `32` and `AXIS_TICK_HZ` as a frozen array holding `100`, `1000` and `10000`, following the frozen-array convention the domain layer already uses for its fixed data lists.

Export `formatTickLabel(hz: number): string` returning a hertz-suffixed integer below one thousand and a kilohertz-suffixed value at or above it, trimming a trailing zero fraction so one thousand reads as a bare `1 kHz`.

Export `hzToFraction(hz: number): number` — the position of a frequency in the display range expressed as a fraction from zero to one, computed as the ratio of the logarithm of the frequency over the floor to the logarithm of the ceiling over the floor. Clamp the input into the display range before taking any logarithm, so a zero, negative, non-finite or out-of-range input can never produce a non-finite result. State in a comment that this clamp is the single place the log-of-zero hazard is closed, and that both the bar layout and the tick-label placement go through it.

Export `barEdgesHz(barCount: number): readonly number[]` returning `barCount + 1` geometrically spaced edges from the floor to the ceiling: the common ratio is the range ratio raised to the reciprocal of the bar count, and edge `i` is the floor times that ratio to the `i`. Force the last edge to the ceiling exactly so accumulated floating-point drift cannot leave the final band short. Throw a `RangeError` naming the received value for a bar count that is not a positive integer, following the domain layer's `RangeError`-with-received-value convention.

Export `binIndexForHz(hz, sampleRate, fftSize, binCount): number` returning the nearest integer bin whose centre frequency is the given hertz — the frequency times the FFT size divided by the sample rate, rounded — clamped into `0..binCount - 1`. Return `0` for a sample rate that is not a positive finite number rather than producing a non-finite index.

Export `interface BarBinRange` with `readonly startBin` and `readonly endBin`, both inclusive, and `buildBarBinRanges(sampleRate, fftSize, binCount, barCount): readonly BarBinRange[] | null`. Return `null` when the sample rate is not a positive finite number — the caller's signal that the audio graph is not up yet. Otherwise compute the bar edges once, map each adjacent edge pair to a start and end bin, and then apply two corrections in this order: raise any start bin below `1` to `1`, so the DC bin is never included in any band; and when a band's end bin has fallen below its start bin — which happens for the lowest bands whose whole width is narrower than one bin — set the end bin equal to the start bin so the band still covers exactly one bin. Document both corrections and why each exists.

Create `spectrum-scale.spec.ts` covering every case in this task's behavior block. Structure the bin-range assertions as invariants over the whole returned list rather than as a table of hardcoded indices, so a future change to the bar count or the display range does not require rewriting the proof — the properties being asserted are monotonicity, in-range-ness, non-emptiness and DC exclusion, and those hold for any parameter choice.
  </action>

  <verify>
    <automated>npm test</automated>
    <automated>npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `npm test` and `npm run lint` exit 0.
    - `grep -cE "^import " src/app/features/playground/visualizer/spectrum-scale.ts` outputs `0`.
    - `spectrum-scale.ts` exports `MIN_DISPLAY_HZ`, `MAX_DISPLAY_HZ`, `SPECTRUM_BAR_COUNT`, `AXIS_TICK_HZ`, `formatTickLabel`, `hzToFraction`, `barEdgesHz`, `binIndexForHz`, `BarBinRange` and `buildBarBinRanges`.
    - A spec asserts `buildBarBinRanges(0, 2048, 1024, 32)` returns `null`, and that the same call with sample rate `44100` returns exactly 32 ranges.
    - A spec asserts over the full returned range list that no `startBin` is `0`, every `startBin` is less than or equal to its `endBin`, every bin index is within `0..1023`, and the list's `startBin` values are non-decreasing.
    - A spec asserts `hzToFraction` returns a finite value within `0..1` for inputs `0`, `-5`, `MIN_DISPLAY_HZ`, `MAX_DISPLAY_HZ` and `48000`.
    - A spec asserts `formatTickLabel(1000)` returns `1 kHz` and `formatTickLabel(100)` returns `100 Hz`.
  </acceptance_criteria>

  <done>A pure module maps hertz to horizontal position and to inclusive frequency-bin ranges, refuses to produce a range list before a real sample rate exists, never includes the DC bin, and is proven by invariant tests that survive a change to the bar count.</done>
</task>

<task type="auto">
  <name>Task 2: Bar rendering and the labelled frequency axis</name>

  <reversibility rating="reversible">Bar geometry, spacing and colours are constants inside the pure draw module; nothing outside it depends on their values.</reversibility>

  <read_first>
    - src/app/features/playground/visualizer/visualizer-frame.ts
    - src/app/features/playground/visualizer/visualizer-frame.spec.ts
    - src/app/features/playground/visualizer/spectrum-scale.ts
    - src/app/core/browser/canvas-2d.token.ts
    - src/app/core/browser/testing/fake-canvas-2d-context.ts
    - src/styles/_tokens.scss
  </read_first>

  <behavior>
    - `drawSpectrum` fills the background once, then emits exactly one filled rectangle per range in the passed list, then draws the axis band.
    - Each bar's rectangle x-positions are non-decreasing across the list and every rectangle lies fully within the geometry's width; no rectangle has a non-finite coordinate or a negative width.
    - A bar whose band's bins are all zero has zero height; a bar whose band contains the byte value 255 reaches the full plot height; a bar takes the maximum, not the mean, of its band, so a band holding `[0, 0, 255, 0]` renders the same height as a band holding `[255]`.
    - `drawFrequencyAxis` emits exactly one text draw per entry in the tick list, with the text equal to that entry's formatted label, and with x-positions ordered the same way the tick frequencies are ordered.
    - The tick label for 1 kHz sits at the horizontal position `hzToFraction(1000)` implies for the geometry's width, to within one pixel — labels and bars are placed by the same mapping.
    - `drawEmptySpectrum` fills the background and draws the axis band with its labels, and emits no bar rectangles.
    - None of the three functions allocates per call: calling each twice with identical arguments produces structurally identical recorded call sequences.
  </behavior>

  <files>src/app/features/playground/visualizer/visualizer-frame.ts, src/app/features/playground/visualizer/visualizer-frame.spec.ts</files>

  <action>
Extend `visualizer-frame.ts`. It must continue to import no symbol from Angular; its only imports are the drawing-surface type from the canvas seam module and the pure scale module from task 1.

Add `SPECTRUM_CSS_WIDTH` at `640` — matching the oscilloscope width so the two lanes align — `SPECTRUM_CSS_HEIGHT` at `200`, and `SPECTRUM_AXIS_BAND_HEIGHT` at `18`, the strip along the bottom reserved for tick labels. The plot height available to bars is the geometry height minus the axis band height; state that relationship in a comment so a future height change cannot leave bars drawing over their own labels.

Add `BAR_FILL_COLOR`, `AXIS_LABEL_COLOR` and `AXIS_LABEL_FONT`, each with a comment naming the design token in `src/styles/_tokens.scss` whose value it mirrors — the bars use the accent colour, the labels use the muted text colour — and following the same mirroring note the existing colour constants already carry.

Export `drawFrequencyAxis(ctx, geometry)`: set the label colour, font and a centred text alignment, then for each entry in the tick list compute its x-position as the geometry width times the scale module's fraction function applied to that frequency, and draw the formatted label near the bottom of the canvas inside the axis band. Save and restore the context around the alignment and font changes so this function cannot leave state behind for the next draw.

Export `drawSpectrum(ctx, data, ranges, geometry)`: fill the whole canvas with the background colour; compute the plot height and a per-bar slot width as the geometry width divided by the range-list length; for each range, scan its inclusive bin span in `data`, take the maximum byte, convert it to a height as that maximum over 255 times the plot height, and fill a rectangle at the bar's slot with a small gap so bars read as discrete columns rather than a solid block, anchored so the bar grows upward from the top of the axis band; then call the axis function. Guard the byte read so an index beyond the passed data's length contributes zero rather than producing a non-finite maximum. Allocate nothing — accumulate the maximum in a local rather than building a per-band array.

Export `drawEmptySpectrum(ctx, geometry)`: background fill plus the axis, no bars. This is the rest state the component draws when the tap reports no live data, and it is what makes the axis labels visible before a note is ever played.

Extend `visualizer-frame.spec.ts` with every case in this task's behavior block, asserting against the recording context double's ordered call log. Assert the maximum-not-mean behaviour with the two explicit band contents named above, since that is the one choice here that a reasonable reader might otherwise assume went the other way.
  </action>

  <verify>
    <automated>npm test</automated>
    <automated>npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `npm test` and `npm run lint` exit 0.
    - `grep -cE "^import .*@angular/core" src/app/features/playground/visualizer/visualizer-frame.ts` outputs `0`.
    - `visualizer-frame.ts` exports `drawSpectrum`, `drawFrequencyAxis` and `drawEmptySpectrum`.
    - A spec asserts `drawSpectrum` emits exactly one `fillRect` per range beyond the background fill, and that every emitted rectangle has a finite, non-negative width and lies within the geometry width.
    - A spec asserts a band containing `[0, 0, 255, 0]` and a band containing `[255]` produce the same bar height.
    - A spec asserts `drawFrequencyAxis` emits exactly three `fillText` calls whose texts are `100 Hz`, `1 kHz` and `10 kHz` in that order.
    - A spec asserts the `1 kHz` label's x-coordinate is within one pixel of `SPECTRUM_CSS_WIDTH * hzToFraction(1000)`.
    - A spec asserts `drawEmptySpectrum` emits zero bar rectangles but still emits the three tick labels.
  </acceptance_criteria>

  <done>The spectrum draws as discrete bars taking each band's peak, the frequency axis carries three drawn tick labels placed by the same logarithmic mapping the bars use, and the rest state still shows the labelled axis.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the second lane into the visualizer — frequency buffer, cached band ranges, accessible descriptions, reduced-motion repaint rate, and the coming-soon trim</name>

  <reversibility rating="reversible">The reduced-motion repaint interval is one constant, and the second canvas is additive markup within one component.</reversibility>

  <read_first>
    - src/app/features/playground/visualizer/visualizer.ts
    - src/app/features/playground/visualizer/visualizer.html
    - src/app/features/playground/visualizer/visualizer.scss
    - src/app/features/playground/visualizer/visualizer.spec.ts
    - src/app/features/playground/visualizer/visualizer-frame.ts
    - src/app/features/playground/visualizer/spectrum-scale.ts
    - src/app/core/audio/synth-engine.ts
    - src/app/core/browser/motion-preference.ts
    - src/app/core/browser/motion-preference.spec.ts
    - src/app/core/browser/animation-frame.token.ts
    - src/app/features/playground/playground.ts
    - src/app/features/playground/playground.spec.ts
    - src/app/features/algorithms/algorithm-diagram/algorithm-diagram.html
  </read_first>

  <behavior>
    - The component acquires a second 2D context for the spectrum canvas through the same injected factory, sizes its backing store by the same device-pixel-ratio rule, and starts no loop when either context is unavailable.
    - The component holds two distinct buffers: one of length `ANALYSER_FFT_SIZE` for the time domain and one of length `ANALYSER_FREQUENCY_BIN_COUNT` for the frequency data. Neither is reallocated across frames and neither is ever passed to the other read method.
    - Band ranges are `null` until the engine reports a positive sample rate; while they are `null` the spectrum draws its empty labelled state. Once built they are cached and `buildBarBinRanges` is not called again on subsequent frames.
    - Ticking the scheduler with live data repaints both lanes on the same tick.
    - With reduced motion preferred, ticks arriving less than the reduced-motion interval apart after a repaint produce no repaint, and a tick at or beyond that interval does; with reduced motion not preferred, every tick repaints. In both cases a frame is re-requested on every tick.
    - The DOM carries a text description for each lane. The oscilloscope description names it as the waveform over time; the spectrum description names the logarithmic frequency range, the three labelled ticks, and that bar height is the loudness of that band. Both descriptions change only with the engine status and never from the frame callback.
    - Each canvas carries an image role and is associated with its description, so assistive technology reads the description rather than an unlabelled graphic.
    - Playground's coming-soon list no longer contains the oscilloscope-and-spectrum entry and still contains the algorithm-selector and operator-strips entries.
  </behavior>

  <files>src/app/features/playground/visualizer/visualizer.ts, src/app/features/playground/visualizer/visualizer.html, src/app/features/playground/visualizer/visualizer.scss, src/app/features/playground/visualizer/visualizer.spec.ts, src/app/features/playground/playground.ts, src/app/features/playground/playground.spec.ts</files>

  <action>
**1. `visualizer.ts` — the second lane.**

Add a required view child for the spectrum canvas against a new template reference. In the existing render hook, acquire and size the spectrum context exactly as the oscilloscope context is acquired and sized, using the spectrum width and height constants; if either context comes back unavailable, draw nothing and start no loop, keeping the existing all-or-nothing posture rather than running a half-live display.

Add a second preallocated buffer as an instance field, sized from the frequency-bin-count constant. Name the two fields so their sizes are unambiguous at every use site — this is the one place the two byte-array reads can be confused, and the boundary double throws on a mismatch precisely so that confusion fails loudly.

Add a private mutable field holding the cached band ranges, initially null. In the frame callback, when the field is still null, ask the tap for its analysis sample rate and try to build the ranges from it, the FFT-size constant, the bin-count constant and the bar count; store whatever comes back. Do not retry more than once per frame and do not rebuild once a non-null list is stored.

Extend the frame callback so that after the time-domain read it performs the frequency read into the frequency buffer, then draws: when live data was read and ranges exist, call the spectrum draw with the frequency buffer, the cached ranges and the spectrum geometry; when live data was read but ranges do not yet exist, or when no live data was read, call the empty-spectrum draw. Keep the existing rest-state flag governing both lanes together so the rest state is still drawn exactly once.

Add the reduced-motion gate. Inject the existing motion-preference service. Add a private field holding the timestamp of the last repaint, initialised so the first tick always repaints. At the top of the frame callback, when the motion preference reports a reduced-motion request and the incoming timestamp is less than the reduced-motion interval past the last repaint, re-request a frame and return without reading or drawing; otherwise record the timestamp and proceed. Export `REDUCED_MOTION_FRAME_INTERVAL_MS` at `100`. Document why the display is throttled rather than frozen: the picture is the pedagogical content, so removing it entirely would remove the feature for the users the rule exists to protect, while a ten-per-second repaint removes the rapid flicker that the rule is actually about. Note that reading the preference signal inside the frame callback creates no reactive dependency, because the callback is not a reactive context — that is what keeps this off the change-detection path.

Add a second status-derived description string as a `computed` for the spectrum, alongside the existing oscilloscope one. Both must describe what the display currently shows, including the non-ready case where the lanes are flat because no audio is running. Neither may be written from the frame callback.

**2. `visualizer.html` / `.scss`.**

Add the spectrum canvas with its template reference, an image role and a label, associated with its own description paragraph, following exactly the markup shape task 1 of plan 10-01 established for the oscilloscope. Keep both lanes unconditionally rendered — no show-or-hide control anywhere in this markup (D-04). Add a short honest caption for the panel stating that this is a live view of the app's own approximated engine output; keep the wording consistent with the approximation label the play surface already renders and do not imply the bars show what a hardware DX7 would produce.

In the stylesheet, give the spectrum canvas its CSS box size from the same nominal dimensions the constants state, stack the two lanes with the existing spacing custom properties, and style the description paragraphs as muted supporting text using the existing colour custom properties. Add nothing that animates, and add no imagery.

**3. `visualizer.spec.ts`.**

Extend the existing suite with every case in this task's behavior block. For the reduced-motion cases, provide a fake matcher through the existing media-query injection token, following the pattern `motion-preference.spec.ts` already establishes, and drive the throttle with explicit timestamps passed to the fake scheduler's tick rather than any real clock. Add a case asserting the frequency buffer handed to the frequency read is a different object from the buffer handed to the time-domain read, and that each has the length its own read requires. Add a case asserting the band-range builder is consulted while the sample rate is zero and not consulted again after a non-null list has been cached. Add a case asserting both description paragraphs are present in the DOM and that each canvas is associated with one.

**4. `playground.ts` / `playground.spec.ts` (D-04).**

Remove exactly the oscilloscope-and-spectrum entry from the coming-soon array. Leave the comparison-tools entry in place — that capability lands in plan 10-04 and removing its entry now would make the page claim something untrue. Leave the algorithm-selector and operator-strips entries untouched; those remain genuine future work. In the spec, add a case asserting the coming-soon list no longer mentions the oscilloscope and still mentions the operator strips, so the trim is pinned rather than incidental.
  </action>

  <verify>
    <automated>npm test</automated>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `npm test`, `npm run lint` and `npm run build` exit 0.
    - `visualizer.html` contains exactly two `canvas` elements, each with `role="img"` and each associated with a description element via `aria-describedby`.
    - A spec asserts both description paragraphs render, and that their text changes between the non-ready and ready audio statuses.
    - A spec asserts the object passed to `readFrequencyInto` is not the object passed to `readTimeDomainInto`, and that their lengths are `1024` and `2048` respectively.
    - A spec asserts that with a zero sample rate the spectrum lane draws its empty labelled state, and that once a positive sample rate is reported the ranges are built once and reused across at least 10 further ticks.
    - A spec asserts that with reduced motion preferred, ticks at timestamps 0, 10, 20 and 30 produce exactly one repaint, and a tick at timestamp 100 produces a second.
    - A spec asserts that with reduced motion not preferred, ticks at timestamps 0, 10, 20 and 30 produce four repaints.
    - `playground.ts`'s coming-soon array no longer contains an entry mentioning the oscilloscope, and still contains the operator-strips entry; a spec pins both.
  </acceptance_criteria>

  <done>Playground shows two live lanes from one analyser tap — a waveform and a labelled logarithmic bar spectrum — each with an accessible description, repainting at a reduced rate when the user asks for reduced motion, with the coming-soon list no longer promising the display as future work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analyser bin data → display geometry | Byte data and a runtime sample rate become canvas coordinates; a non-finite intermediate here produces a silently blank or corrupt display rather than an error. |
| Browser motion preference → repaint rate | An OS-level accessibility preference reaches an imperative loop; failing to honour it is an accessibility defect, not just a performance one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Denial of Service | Logarithmic axis mapping at 0 Hz | medium | mitigate | `hzToFraction` clamps its input into the display range before any logarithm, and `buildBarBinRanges` raises every band's start bin to at least 1 so the DC bin is never mapped. Pinned by task 1's non-finite-input and DC-exclusion cases; without this a single `NaN` x-coordinate blanks the whole lane with no thrown error to notice. |
| T-10-08 | Denial of Service | Band ranges built from a zero or non-finite sample rate | medium | mitigate | `buildBarBinRanges` returns `null` rather than a `NaN`-filled list, and the component treats `null` as "draw the empty labelled state and try again next frame". Pinned by task 1's `null` case and task 3's zero-sample-rate case. |
| T-10-09 | Tampering | Frequency and time-domain buffers confused at a call site | medium | mitigate | Two separately named, separately sized instance fields, plus the boundary double's `RangeError` on any wrong length from plan 10-01. Pinned by task 3's buffer-identity and length case. |
| T-10-10 | Denial of Service | Full-rate animation under a reduced-motion request | low | mitigate | The frame callback gates on the motion-preference signal and the scheduler-supplied timestamp, repainting at most ten times a second. Pinned by task 3's throttle cases in both preference states. |
| T-10-11 | Repudiation | Display implying hardware-accurate DX7 analysis | low | mitigate | The panel caption states plainly that this is a live view of the app's own approximated engine, consistent with the approximation label the play surface already renders; carried as a judgment-tier prohibition and re-checked at plan 10-04's checkpoint. |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero packages — the logarithmic axis math is roughly ten lines owned directly rather than pulled from a charting library, per `10-RESEARCH.md`'s "Don't Hand-Roll" disposition. |

</threat_model>

<verification>
- `npm test`, `npm run lint` and `npm run build` all green.
- `spectrum-scale.ts` imports nothing at all; `visualizer-frame.ts` imports no Angular symbol.
- Both canvases render unconditionally in every audio status, each with a text description.
- The coming-soon list no longer promises the oscilloscope and spectrum display.
</verification>

<success_criteria>
- The spectrum renders as bars on a logarithmic axis with three drawn tick labels, from the same analyser tap the oscilloscope uses.
- The DC bin is never mapped and no non-finite coordinate is ever drawn.
- Band ranges derive from the engine's reported sample rate, are built once, and are reused.
- Both lanes carry accessible text descriptions and honour the reduced-motion preference.
</success_criteria>

<output>
Create `.planning/phases/10-visualizers-and-comparison-tools/10-02-SUMMARY.md` when done
</output>
